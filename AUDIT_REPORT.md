# PANDORA 아카데미 + 공용 엔진 설계 감사 보고서

에이전트 10개 / 16분 / 64만 토큰 / 결함 54건


---

## CRITICAL

### 아카데미 시작 시각 '입학식 아침'에 "N일차"가 없어 시간 시스템 전체(v315~v318)가 잠들어 있다

- **위치**: novel.html:8003 academy initialState / 11804 timeFlowNudge 게이트 / 11816 advanceTimeSlotIfStale 정규식 / 4508 경과일 주입
- **증상**: 입학 직후 구간에서 앱은 시간에 대해 아무 말도 못 한다 — 모델이 '입학식 아침'을 계속 복사하면 몇십 턴을 아침에 머물고, 4508의 '지금은 N일차다' 교정도 안 실려 매 턴 '오늘 입학식' 프레이밍이 반복된다. 그 상태에서 15턴째에 '입학 1개월 후 서열전'이 주입돼, 화면에는 여전히 입학식 아침인데 한 달 뒤 사건이 터진다.
- **근거**: academy initialState는 `time: '입학식 아침'`(8003). 그런데 timeFlowNudge는 `if (!/\d+\s*일차/.test(cur)) return '';`(11804)로 시작하고, advanceTimeSlotIfStale은 `/(\d+)\s*일차\s*(\S+)?/`가 매칭돼야만 동작하며(11816-11817), buildStatePrompt의 경과일 주입도 `/(\d+)\s*일차/`(4508)를 요구한다. 즉 세 장치 전부가 "N일차" 문자열에 게이트돼 있는데 아카데미는 그 문자열 없이 시작한다. 모델이 자발적으로 '2일차 오전' 형식을 채택하기 전까지 넛지도 백스톱도 경과일 교정도 0이다. 반면 이벤트 주입(dueEventNow)은 turnCount 기반이라 시간 표기와 무관하게 진행된다.
- **수정**: academy initialState.time을 '1일차 아침 (입학식)'처럼 파서가 읽는 형식으로 바꾸고, 모델이 어떤 표기를 내든 일차+슬롯을 추출·정규화해 gameState.time에 다시 써 넣는 normalizeTime()을 s.time 수신부(11864)에 둔다. 표기 규칙은 ENGINE_RULES의 time 필드 설명에도 'N일차 <아침|오전|오후|저녁|밤>' 고정으로 못 박을 것.

### s.time을 값 비교 없이 무조건 resetTimeSlot — 모델이 같은 시각을 에코하면 넛지·백스톱이 영원히 안 뜬다

- **위치**: novel.html:11864 `if (s.time && !_isIdol) { gameState.time = s.time; resetTimeSlot(); }` / 11799-11800 / 2366 엔진 규칙
- **증상**: 모델이 시각 문자열을 복사만 하면 앱은 '시간이 흘렀다'고 오인해 카운터를 0으로 되돌린다 — '이 시간대가 N턴째'라는 넛지(3턴)도, 백스톱(10턴)도 발동 조건에 도달하지 못한다. v315가 고치려던 원래 증상(54턴에 2일차, 발동 사건 0)이 v318 구조에서 그대로 재발할 수 있는 경로다.
- **근거**: 수신부에 이전 값과의 비교가 없다. resetTimeSlot()은 `_slotTurns = 0; _slotMoved = false`(11800)다. ENGINE_RULES_PROMPT 2366은 time을 적극적으로 갱신하라고 강하게 지시하므로 모델이 매 턴 time 필드를 채울 유인이 크고, 갱신할 게 없으면 현재 값을 그대로 되뱉는 것이 LLM의 일반적 거동이다. 게다가 s.location이 s.time보다 먼저 처리되어(11857) `_slotMoved = true`가 세워진 직후, 같은 턴의 time 에코가 그 플래그까지 지운다.
- **수정**: `if (s.time && s.time !== gameState.time)`로 실제 변화가 있을 때만 갱신+리셋하고, 동일 문자열이면 갱신 없음으로 취급해 _slotTurns를 유지한다. 슬롯 단위 비교(정규화 후 day/slot 튜플 비교)로 하면 '3일차 오후'와 '3일차 오후(연무장)' 같은 변주도 흡수된다.

### choices 규칙이 정면 충돌한다 — "빈 배열 절대 금지"와 "전투에선 반드시 빈 배열"이 같은 프롬프트에 동시 존재

- **위치**: novel.html 2325행(ENGINE_RULES 분량·구성 규칙) vs 2383행(출력 스키마 choices) vs 2447행(ENGINE_MOD_RPG) + 10593~10607행(PRIMING_SHOTS)
- **증상**: 비전투 장면에서도 선택지가 빈 채로 와서 입력창만 남는다(유저가 이미 지적한 증상). 반대로 서열전·결투 중에는 선택지가 나와서 판정의 의미가 사라진다. 아카데미는 서열전·결투가 핵심이라 두 오작동을 다 겪는다.
- **근거**: 실제로 읽은 세 문장이 서로를 부정한다. ①2325행: "★★choices를 절대 생략하지 마라. 빈 배열·null도 금지. 응답마다 최소 2개를 반드시 채워라. … 장면이 어떤 상태든 — 정적이든 회의든 이동 중이든 — 다음 한 걸음 두 갈래는 언제나 만들 수 있다." ②2383행(스키마): "★전투·충돌·판정이 걸린 장면에서는 반드시 빈 배열 [] 로 두어라." ③2447행(RPG 모듈, academy는 RPG_STORIES 소속이라 11357행에서 주입됨): "★전투 중 선택지 금지 … choices를 빈 배열로 두고." 두 지시 모두 '절대/반드시' 강도이고 예외 조항이 서로 없다. 더 나쁜 것은 10593~10607행 PRIMING_SHOTS의 assistant 예시가 `"choices": []`로 끝난다는 점 — 프롬프트 맨 앞의 few-shot이 '금지된 행동'을 시연하고 있다. 규칙 문장보다 예시의 영향력이 크므로, 2325행이 인용한 유저 지적("가끔 선택지 안 나오고 채팅만 입력됨", 2026-07-28)의 잔존 원인이 이 예시일 개연성이 높다(이 인과는 추론이다 — 실제 응답 로그로 확인 필요).
- **수정**: 규칙을 한 곳으로 합치고 조건을 배타적으로 쓴다: 2325행을 "판정이 걸린 장면(전투·충돌·추격)이 아니면 choices를 절대 생략하지 마라 — 그 외 모든 장면에서 최소 2개"로 고치고, 2383·2447행 중 하나만 남긴다. PRIMING_SHOTS의 예시 choices를 실제 선택지 2개가 든 배열로 교체한다(빈 배열 시연 제거).

### 도래한 사건이 '프롬프트를 조립한 순간' 발동 처리돼 소비된다 — 모델이 무시해도, 재시도해도 영구 소실

- **위치**: buildStatePrompt() 4667~4690행 / callGemini 재시도 경로 11552·11561·11574·11590·11636행
- **증상**: '신입생 서열전', '학원제 — 여명회 습격' 같은 1부 핵심 사건이 화면에 한 번도 안 나온 채 '이미 일어난 것'으로 기록된다. v315/v316이 고치려던 증상("스토리가 없다")이 그대로 재발하며, 이번엔 상태창상 사건이 소진돼 있어 원인 추적도 어렵다.
- **근거**: 4682~4686행: 사건 블록을 문자열에 붙인 **직후** `gameState.firedEvents.push(_due.t)`와 `gameState._lastEventTurn = turnCount`를 실행한다. 모델이 실제로 그 사건을 장면으로 연출했는지는 어디서도 검증하지 않는다(processResponse 쪽에 firedEvents 검증 코드 없음). 게다가 callGemini는 실패 시 `return callGemini(activeCharIds, retryCount + 1)`로 자기 자신을 재호출하고(11552 등 5곳), 재호출은 buildStatePrompt를 다시 돌린다 — 이때 dueEventNow()는 4477행 `!fired.includes(e.t)` 필터에 걸려 null을 반환하므로, **실제로 전송되는 재시도 요청에는 사건 블록이 아예 빠진다**. 또 EVENT_MIN_GAP=8(4453행) 때문에 소실된 사건은 최소 8턴 뒤에나 '다음 사건'으로 넘어가고, 소실분 자체는 영원히 돌아오지 않는다. 15190·15406행의 되감기(turnCount 감소)도 firedEvents를 복원하지 않는다.
- **수정**: 발동 기록을 프롬프트 조립이 아니라 **응답 수신 후**로 옮긴다. buildStatePrompt는 `gameState._pendingEvent = _due.t`만 세팅하고, processResponse에서 응답의 event/chronicle/narration에 해당 사건 흔적이 있을 때만 firedEvents에 push한다(없으면 다음 턴에 재주입). 최소한 retryCount>0인 재호출에서는 _pendingEvent를 그대로 재사용하도록 dueEventNow에 예외를 둔다.

### 모델이 매 턴 쓰는 chronicle을 앱이 통째로 버린다 — 장기 기억의 유일한 입력 경로가 끊겨 있다

- **위치**: novel.html:2384(스키마) / 2395(규칙문) vs processResponse() 11857~12320 — data.chronicle을 읽는 코드 없음
- **증상**: 플레이어가 대화로 정한 설정(이름·과거·유품·약속), 밝혀진 비밀, 관계 전환 — 규칙이 '영구 보존된다'고 약속한 그 모든 항목이 생성 즉시 소멸한다. 모델은 매 턴 이 필드를 채우느라 토큰을 쓰지만 결과는 저장도 재주입도 되지 않는다. 몇십 턴 뒤 같은 사실을 모순되게 다시 쓰는 '설정 붕괴'의 1차 원인.
- **근거**: 응답 스키마 최상위에 "chronicle": [...] 필드가 있고(2384), 엔진 규칙은 '반드시 그 턴 chronicle 배열에 1줄로 기록하라 — 이것이 장기 기억이다'(2395)라고 못박는다. 그런데 processResponse가 실제로 소비하는 필드는 background/cg/mood/ending/state_update/dialogue/narration/choices 뿐이다(11857~12320 구간 data.* 전수 추출로 확인). 파일 전체에서 'chronicle'은 12곳뿐이고, 쓰기(push)는 summarizeIfNeeded의 요약 패스 한 곳(10839~10841)이 유일하다. 스트리밍 경로(extractStreamUnits)에도 없다.
- **수정**: processResponse의 state_update 처리 블록 옆에 data.chronicle 수집을 추가한다: 배열이면 문자열 항목만, 앞에 현재 gameState.time을 붙여(예: '6일차 오후: …') gameState.chronicle에 push, 정규화 문자열 기준 중복 제거, 상한 유지. replaying일 때는 push하지 말 것(되감기 재생 시 중복 누적 방지).

### 요약 게이트가 구조적으로 자기 모순 — 원문 총량이 12만 자를 넘기 전까지 요약·연대기가 단 한 번도 안 돈다(그리고 같은 이유로 HISTORY_MAX도 죽는다)

- **위치**: recentWindowCount() 2188~2198 / summarizeIfNeeded() 10753~10758 / trimHistory() 10192~10201 / autoSave() 10204~10206
- **증상**: 게임 초반 수십 턴 동안 장기 기억 장치가 하나도 작동하지 않는다. 연대기 탭은 계속 비어 있고, 요약도 없고, 원문만 무제한으로 불어난다. 그동안 매 턴 12만 자에 육박하는 원문을 통째로 재전송하므로 응답이 갈수록 느려진다. 그리고 임계를 넘는 순간 갑자기 요약이 시작되면서 '지금까지 없던 요약'이 프롬프트 앞에 끼어들어 화법이 한 번 튄다.
- **근거**: 실제 코드를 읽고 대입한 결과다(추측 아님). recentWindowCount는 budgetChars = min(RECENT_CHARS_CAP 120000, ctx*0.40)로 뒤에서부터 누적하다가 `chars > budget && count >= 10`일 때만 break한다. 원문 총량이 12만 자 미만이면 루프가 끝까지 돌아 count = gameHistory.length, 즉 _win === gameHistory.length를 반환한다. 그 값이 그대로 summarizeIfNeeded의 가드에 들어간다: `if (gameHistory.length - summaryIndex < 8 + _win) return;` → `len - summaryIndex < 8 + len` → `summaryIndex > -8` → summaryIndex가 0 이상인 한 **항상 참**이라 무조건 early return이다. 결과가 두 겹으로 번진다. ①storySummary와 gameState.chronicle을 만드는 유일한 코드가 summarizeIfNeeded 안(10817, 10829~10831)이므로 둘 다 영원히 빈 상태다. ②trimHistory는 `drop = Math.min(over, Math.max(0, summaryIndex))`인데 summaryIndex가 0이라 drop=0을 반환한다 — autoSave가 매 턴 `if (gameHistory.length > HISTORY_MAX) trimHistory(HISTORY_MAX)`를 불러도 아무것도 안 잘린다. HISTORY_MAX=60은 이 상태에서 완전한 사문(死文)이고, 주석 '프롬프트는 최근 8개만 쓴다'(10181)도 현행 코드와 무관하다. 교착은 원문이 12만 자를 넘는 순간에만 스스로 풀린다(턴당 1,000~2,000자면 대략 60~120엔트리 = 30~60턴 — 이 환산은 저장 주석의 '턴당 약 900자'와 유저가 보고한 세이브 200~330KB에서 역산한 추정이다).
- **수정**: 프롬프트 전송창(recentWindowCount)과 요약 경계를 다른 축으로 분리한다. 요약 보존 창을 별도 상수(예: const SUMMARY_KEEP = 16)로 고정하고 가드를 `gameHistory.length - summaryIndex >= SUMMARY_INTERVAL + SUMMARY_KEEP`로, toSummarize도 `slice(summaryIndex, len - SUMMARY_KEEP)`로 바꾼다. 전송창은 지금처럼 char budget으로 크게 잡아도 되지만, 요약·연대기는 그와 무관하게 8턴마다 돌아야 한다. 동시에 trimHistory가 summaryIndex에 목매지 않도록 쿼터 경로용 강제 절단 모드를 둔다.

### 모델이 매 턴 출력하는 chronicle 배열을 앱이 읽지 않는다 — '이것이 장기 기억이다'라고 강제해놓고 전량 폐기

- **위치**: 출력 스키마 2384 / 상태추적 규칙 2395 / processResponse의 state_update 병합부 11840~12010(여기에 chronicle 처리가 없음)
- **증상**: 유저가 대화로 정한 설정(주인공 검이 아버지 유품이라는 것, 정정한 이름·과거·관계)과 서사 확정 사실(약속·비밀 공개·죽음)이 영구 보존되지 않는다. 규칙이 명시한 '기록 안 하면 소멸한다'가 앱 때문에 그대로 실현된다. 유일한 대체 경로인 요약 패스의 기록원 프롬프트는 '실제로 일어난 확정 사건만, 20자 내외, 해석 금지'라 유저가 정한 설정 같은 건 애초에 뽑지 않는다. 100턴쯤 가면 '전에 내가 정했잖아'가 통하지 않는다.
- **근거**: 파일 전체 grep('chronicle') 결과가 2384, 2395, 4444, 4657, 10378, 10829~10831, 15073 — 이게 전부다. 2384는 AI에게 매 턴 `"chronicle": [...]`를 내라는 스키마이고 2395는 '★★확정 사실은 chronicle에 … 이것이 장기 기억이다. 기록 안 하면 몇 턴 뒤 그 사실은 소멸한다'는 규칙이다. 그런데 gameState.chronicle에 실제로 push하는 코드는 10830 한 곳뿐이고, 그건 요약 패스 안의 별도 llmSmall('사건 기록원') 호출 결과다. processResponse는 s.chars / s.player / s.flags / s.war / s.sim / s.event는 병합하지만 data.chronicle도 s.chronicle도 손대지 않는다. 즉 모델이 규칙대로 적어 보낸 확정 사실은 JSON.stringify로 gameHistory에 원문으로만 남았다가 창 밖으로 밀려나면 그대로 사라진다.
- **수정**: processResponse의 state_update 처리 직후에 `if (!replaying && Array.isArray(data.chronicle)) data.chronicle.forEach(c => { if (typeof c === 'string' && c.trim() && !gameState.chronicle.includes(c)) gameState.chronicle.push(c); })`를 넣는다. replaying 가드는 필수(되감기·이어하기 재생에서 중복 누적 방지). 넣고 나면 요약 패스의 기록원 호출은 중복이니 축소하거나 '요약 구간에서 빠진 것만 보완'으로 역할을 바꾸는 편이 낫다.


---

## HIGH

### 백스톱의 '장소 이동' 증거가 sticky라 대화 도중에 시간이 밀린다 — v318이 막으려던 사고가 그대로 재현 가능

- **위치**: novel.html:11814-11828 advanceTimeSlotIfStale / 11857 `_slotMoved = true` / 12003-12005 호출부
- **증상**: 밤에 기숙사 방으로 들어가고(이동 1회 = 증거 확보) 그 방에서 10턴 넘게 대화·정사 장면을 이어가면, 모델이 time을 안 낸 사이 11번째 턴에 앱이 밤→다음날 아침으로 밀어버린다. 화면엔 대사 도중 '🕐 4일차 아침' 토스트가 뜨고 다음 턴 프롬프트에는 아침이 실려 모델이 장면을 억지로 봉합한다 — 정확히 v315에서 사고로 기록된 '대화 중 아침이 오는' 증상이다. 장면이 길어지는 곳(정사·심문·회의)이 정확히 이 조건을 만족한다.
- **근거**: _slotMoved는 s.location이 바뀐 턴에 true가 되고, 이후 resetTimeSlot()이 불릴 때까지 계속 true로 남는다(11800이 유일한 해제 지점). 백스톱은 `_slotTurns >= 10 && _slotMoved`만 보고 '지금 이 턴에 장면이 바뀌었는가'는 보지 않는다. 주석은 '장소 이동 = 장면이 실제로 바뀌었다는 유일한 증거'라고 적었지만, 실제로 검사하는 건 '지난 시간대 갱신 이후 언젠가 한 번 이동했는가'다.
- **수정**: _slotMoved를 '이번 턴에 이동했다'로 좁힌다 — 매 턴 처리 시작에서 false로 초기화하고, 백스톱은 (이번 턴에 이동함 && _slotTurns>=10)일 때만 민다. 대안: 마지막 이동 턴을 _slotMoveTurn에 기록하고 `turnCount - _slotMoveTurn <= 1`을 조건에 추가.

### firedEvents를 프롬프트 조립 시점에 기록 — 재시도·에러·재생성·언두에서 사건이 영구 소실된다

- **위치**: novel.html:4678-4681(주입 즉시 push) / 11401-11405(dynamicCtx는 messages 사본에만) / 11633-11635(소프트 거부 재시도) / 11661(catch) / 15188-15190 regenerate / 15404-15406 undoInput
- **증상**: 플레이어가 응답이 맘에 안 들어 '다시 생성'을 누르는 흔한 행동 하나로 신입생 서열전이나 학원제가 영원히 안 일어난다. 통신 오류 한 번도 같은 결과다. 모델이 그 턴에 사건을 미루기만 해도(다음 턴엔 지시가 없으니) 동일하게 실종된다. 유저 입장에선 '스토리가 없다'가 다시 재현되고, 원인은 화면에 아무 흔적도 남지 않는다.
- **근거**: buildStatePrompt 안에서 사건 문단을 문자열에 붙이자마자 `gameState.firedEvents.push(_due.t)`와 `_lastEventTurn = turnCount`를 실행한다(4678-4681). 응답 성공 여부는 확인하지 않는다. 그 결과: ①소프트 거부 시 `callGemini(activeCharIds, retryCount+1)`(11635)가 프롬프트를 다시 만드는데 이미 fired라 사건 문단이 빠진 프롬프트로 재시도된다 ②fetch 예외/타임아웃(11661)이면 아무 장면도 안 나온 채 사건만 소모된다 ③regenerate()·undoInput()은 turnCount만 되돌리고(15190/15406) firedEvents·_lastEventTurn은 그대로 둔다. 또한 주입문은 buildMessages()가 만든 배열 사본에만 붙고 gameHistory 원본에는 안 남으므로(11402-11405) 다음 턴엔 지시가 사라진다 — '늦어도 다음 한두 턴 안에'라는 문구는 지킬 수단이 없다.
- **수정**: fired 커밋을 응답 성공 이후(processResponse 진입 시점)로 옮긴다. 더 안전하게는 gameState.pendingEvent에 넣어두고, 모델이 그 사건을 실제로 다뤘다는 신호(state_update.event/flags/chronicle에 해당 키워드)가 잡힌 턴에만 firedEvents로 승격하고 미승격이면 다음 턴에 같은 문단을 다시 싣는다. 최소한 regenerate/undoInput에서 firedEvents·_lastEventTurn을 스냅샷으로 롤백할 것.

### 숫자 없는 cond('학원제 직후','개전 직후')는 앱이 영영 안 꺼낸다 — 2부 개전·진영 선택이 자동으로 열리지 않는다

- **위치**: novel.html:4454-4461 eventDueTurn(`return null`) / 4472 필터 / 2941-2942 ACADEMY_EVENTS
- **증상**: 학원제(45턴)가 1부의 끝으로 터진 뒤 앱이 다음으로 꺼내는 사건은 이졸데 폭주(90턴)뿐이다. 그 사이 45턴 동안 개전도, 진영 선택 choices도, 전황판도 열리지 않는다 — 1부는 끝났는데 2부가 시작되지 않는 표류 구간이 생기고, 유저가 직접 '진영을 고르겠다'고 입력하지 않으면 국가전 콘텐츠 전체가 잠긴 채로 남는다.
- **근거**: eventDueTurn은 개월·주·일차 세 패턴만 숫자로 환산하고 나머지는 null을 반환한다(4461). dueEventNow는 `.filter(x => x.due !== null && ...)`로 null을 전부 버린다(4472-4473). ACADEMY_EVENTS 9개 중 '개전 — 진영 선택'(cond '학원제 직후')과 '완충령의 선택 — 가주의 항복 노선'(cond '개전 직후 (진영 선택 이후)')이 여기에 걸린다. 개전 이벤트는 faction을 choices로 받아 state_update.faction에 기록시키는 유일한 지시이고, faction이 처음 기록되는 순간이 initWar() 트리거다(11873-11877).
- **수정**: 체인 조건을 지원한다 — 이벤트에 `after: '학원제 — 여명회 습격'`(+오프셋 턴) 필드를 추가하고, eventDueTurn이 선행 이벤트의 실제 발동 턴(firedEvents 기록 시 턴도 함께 저장)에 오프셋을 더해 due를 계산하게 한다. 최소 조치로 '개전'만이라도 학원제 fired 이후 EVENT_MIN_GAP 턴 뒤 due로 하드코딩할 것.

### 기존 세이브에서 밀린 사건이 8턴 간격으로 연쇄 발동 + 매번 서사시간 점프를 강제한다

- **위치**: novel.html:4463-4478 dueEventNow / 4455 EVENT_MIN_GAP=8 / 4453 TURNS_PER_MONTH=15 / 4671-4675 주입문
- **증상**: 이어하기 한 번에 1개월→2개월→3개월이 16턴 만에 소모되고 1부가 끝난다. 매 사건마다 '그 사이 한 달이 지났다'는 서너 줄 요약으로 관계·성장 구간이 통째로 스킵되므로, 플레이어는 자기가 겪지 않은 한 달을 세 번 통보받는 느낌을 받는다. 페이싱을 늘리려던 v316 환산이 오래된 세이브에서는 정반대로 작동한다.
- **근거**: firedEvents가 비어 있는 기존 세이브(주석이 인용한 실측 54턴)를 열면 due<=54인 미발동 사건이 전부 후보가 된다: 서열전(15)·본가 소환장(30)·학원제(45). dueEventNow는 그중 가장 이른 하나만 내보내지만 게이트는 EVENT_MIN_GAP 8턴뿐이라 55턴·63턴·71턴에 차례로 나온다. 게다가 주입문은 '조건에 적힌 서사 시간과 지금 일차가 맞지 않으면 그 사이 경과를 서너 줄로 요약하고 시간을 사건 시점으로 건너뛰라'고 매번 지시한다(4674).
- **수정**: 세이브 로드 시 마이그레이션: 현재 turnCount 기준으로 이미 지났어야 할 사건은 가장 최근 1개만 남기고 나머지는 firedEvents에 스탬프(겪은 것으로 간주)한다. 또는 due를 절대 턴이 아니라 '직전 사건 발동 턴 + TURNS_PER_MONTH×간격'으로 재스케줄해 밀린 총량이 누적되지 않게 한다.

### 시간축과 사건축이 서로 30배 어긋나게 캘리브레이션돼 있다 (15턴=1일 vs 15턴=1개월)

- **위치**: TIME_SLOTS 11789행 / TIME_SLOT_NUDGE 11797행 / timeFlowNudge 11804~11812행 vs TURNS_PER_MONTH 4452행 / eventDueTurn 4454~4462행
- **증상**: 쌓아온 15턴치 일상(수업·관계·복선)이 한 줄 요약으로 증발한 뒤 사건이 시작된다. 유저 체감은 '갑자기 한 달이 지나 있고, 하던 이야기가 사라짐'이다. v318이 고친 "대화 중 아침이 오는" 사고의 시간 단위만 커진 판본이다.
- **근거**: 두 상수를 같이 읽으면 계산이 맞지 않는다. 시간 쪽: TIME_SLOTS는 5칸(아침·오전·오후·저녁·밤), TIME_SLOT_NUDGE=3이라 모델이 넛지를 따르면 시간대 하나가 약 3턴 → **하루 ≈ 15턴**. 사건 쪽: TURNS_PER_MONTH=15이므로 '입학 1개월 후' 이벤트는 **15턴째에 도래**한다. 즉 앱이 스스로 만든 두 시계가 30배 차이가 난다. 실제로 15턴째면 time은 대략 '2일차'인데 그 턴에 '입학 1개월 후 신입생 서열전'이 도래하고, 4676행 지시가 "조건에 적힌 서사 시간과 지금 일차가 맞지 않으면 그 사이의 경과를 서너 줄로 요약하고 시간을 사건 시점으로 건너뛴 뒤 장면을 열어라"라고 말한다 — 한 턴에 28일을 요약으로 날리라는 뜻이고, 이후 이벤트마다 이 점프가 반복된다.
- **수정**: 둘 중 하나를 기준으로 통일한다. (a) 사건축을 시간축에 맞춘다: eventDueTurn을 턴이 아니라 gameState.time의 일차로 판정하고, TURNS_PER_MONTH 대신 '경과 일차 ≥ 30' 같은 조건을 쓴다. (b) 시간축을 사건축에 맞춘다: 시간대를 밀 때 하루가 아니라 주 단위로 넘어가는 스토리(학원물)용 페이싱을 두고 TIME_SLOT_NUDGE를 늘린다. 어느 쪽이든 '한 턴 = 서사 며칠'을 한 곳에서만 정의해야 한다.

### 친밀도 사다리가 서로 다른 문턱으로 3중 주입된다 — 같은 수치에 대해 세 지시가 엇갈린다

- **위치**: ENGINE_RULES 2394행 / buildStatePrompt B-2 블록 4519~4529행 / 관계 단계 상태머신 4643~4652행
- **증상**: 관계가 진전돼도 태도가 안 변하거나(보수적 지시 채택), 반대로 단계를 건너뛴 스킨십·고백이 튀어나온다. 유저가 과거에 지적한 "친해져도 태도가 그대로"와 "갑자기 좋아함"이 같은 원인에서 번갈아 나온다.
- **근거**: 매 턴 프롬프트에 세 개의 구간표가 동시에 들어간다. ①2394행: 0~20 경계 / 21~40 호감 / 41~60 신뢰·비밀 일부 공유 / 61~80 애정·독점욕·스킨십 / 81+ 연인. ②4521~4528행: intimacy<40 = "아직 남인 사람 — 설렘·질투·독점욕·스킨십·고백 금지", ≥40 = "신뢰(비밀 일부 공유)", ≥70 = "애정(독점욕·스킨십)". ③4645~4650행: ≤20 [경계] / ≤45 [탐색] "비밀·상처는 굳게 닫혀 있다" / ≤70 [신뢰] "약점의 가장자리" / 71+ [유대]. 문턱이 40·60·80 / 40·70 / 20·45·70으로 전부 다르다. 구체적 충돌: 친밀도 42면 ①·②는 "비밀 일부 공유", ③은 "비밀·상처는 굳게 닫혀 있다". 친밀도 65면 ①은 "애정·독점욕·스킨십", ②·③은 "신뢰" 단계라 스킨십 금지에 가깝다. 마일스톤 트리거도 ①은 "40·70 돌파"인데 ③의 경계는 45·70이라 어긋난다.
- **수정**: 구간표를 코드 한 곳(상태머신)에만 두고 ENGINE_RULES 2394행의 구간 나열은 삭제한다. B-2 블록(4519~4529)은 상태머신이 이미 출력하는 정보의 중복이므로 제거하거나, 상태머신과 같은 상수(20/45/70)를 참조하게 바꾼다.

### "주인공 대필 절대 금지(위반=실격)"와 "주인공 내면 독백을 넣어라"가 동시에 지시된다

- **위치**: ENGINE_RULES 2312행 vs 2319행 / 2330행 / ACADEMY_LORE 2900행
- **증상**: 모델이 2312행(위반=실격 문구가 붙어 가장 강함)을 우선하면 주인공 독백이 사라져 빙의물의 재미인 '나만 아는 원작 지식'이 화면에 안 나온다. 반대로 2319행을 따르면 주인공의 감정·결정까지 대신 써서 주도권을 뺏는다. 두 실패 모드가 턴마다 무작위로 섞인다.
- **근거**: 2312행은 최고 강도로 금지한다: "★★주인공 대필 절대 금지(위반=실격): 어떤 상황에서도 주인공(플레이어)의 대사·생각·감정 변화·결정·행동을 당신이 대신 쓰지 마라." 그런데 같은 프롬프트의 세 곳이 정반대를 요구한다. 2319행: "등장인물이 없는 장면이면 주인공의 짧은 내면 독백을 주인공 이름 speaker(char_id null)로 넣어 살아있게 만들 것." 2330행: "(전지적 누설 금지) … 예외는 주인공 자신의 내면뿐이다." 그리고 아카데미 헌법 2900행: "이 개념들(소설·원작·빙의)은 오직 주인공의 속마음·독백·나레이션에서만 등장할 수 있다" — 즉 아카데미의 핵심 장치인 원작 지식 연출은 주인공 독백을 쓰지 않으면 성립하지 않는다. 2312행에는 '내면 독백은 예외'라는 단서가 없고, 2319행에는 '대필 금지의 예외'라는 표시가 없다.
- **수정**: 2312행에 예외를 명시적으로 붙인다 — "단 주인공의 **관찰·회상·원작 지식 인출**은 허용하고(speaker=주인공, char_id null), **결정·감정 변화·다음 행동 선언**은 금지한다." 그리고 2319·2330행이 그 예외를 참조하도록 문구를 맞춘다.

### 아이돌 전용 규칙 17,904자가 아카데미 시스템 프롬프트에 매 턴 실린다 (오배송)

- **위치**: callGemini 11358행 `if (_sidNow !== 'lovediary') worldPrompt += ENGINE_MOD_CG;` / ENGINE_MOD_CG 2452~2588행
- **증상**: 아카데미 프롬프트의 약 4분의 1이 무관한 지시다. 캐시가 걸려 비용은 줄어도 모델의 주의 예산은 그대로 소모되고, 스탯·이름 창작 규칙이 모듈 간에 어긋나 판정 표기가 빠지거나 로스터 밖 인물이 등장한다.
- **근거**: 11358행의 조건은 'lovediary가 아니면' 하나뿐이라 academy도 ENGINE_MOD_CG 전체를 받는다. 그런데 그 모듈의 2454~2576행(측정 17,904자)은 스스로 "## 아이돌 육성 sim 필드 (스토리 「무대 뒤 3분」 전용 — 다른 스토리에선 절대 쓰지 마라)"라고 선언한 내용이다: 광고 계약 금액 처리, 주간일보 지면, 기자 문지효 로비, 무대의상 1~6번, 팬덤명, 담보대출까지. 실제로 CG 규칙(2577~2587행)은 2,137자뿐이다. 부작용은 단순 낭비에 그치지 않고 모순을 만든다 — 2588행 근처 "★숫자는 절대 네가 만들지 마라. 팬 수·자금·점수는 앱이 계산해서…"는 ENGINE_MOD_RPG 2437~2441행의 "스탯 초기값을 부여하라 / xp를 올려라"와 정면으로 부딪히고, 2564~2566행 "목록에 없는 새 인물이 필요하면 만들되 sim.person으로 등록하라"는 2333행 "★이름 있는 인물을 새로 지어내지 마라"와 부딪힌다.
- **수정**: ENGINE_MOD_CG를 두 개로 쪼갠다 — ENGINE_MOD_IDOL(2454~2576행)은 `_sidNow === 'idol'`일 때만, ENGINE_MOD_CG(2577~2587행)는 지금 조건 그대로 주입. RPG_STORIES처럼 IDOL 전용 게이트를 명시적으로 둔다.

### v317 NPC 능동성 규칙이 아카데미 '무명 원칙'과 매 턴 슬롯을 두고 싸운다

- **위치**: ENGINE_RULES 2399~2404행(v317) vs ACADEMY_LORE 2894~2897행(무명 원칙·과소평가 관성) vs getCharPrompt 4294행
- **증상**: 망나니에게 매 턴 누군가 말을 걸고 부탁·경고를 남기는 장면이 반복돼, 이 스토리의 핵심 재미(아무도 주목하지 않는 위장)가 무너진다. 반대로 무명 원칙을 지키면 v317이 고치려던 정적인 장면으로 돌아간다.
- **근거**: 질문하신 충돌은 '주도권 보호·대필 금지'와는 아니다 — 2404행이 "단 주도권은 여전히 플레이어의 것 … 주인공이 무엇을 하고 무엇을 말할지는 절대 대신 쓰지 마라"로 스스로 방어하고 있어 그쪽은 정합적이다. 실제 충돌은 **스토리 헌법과의 충돌**이다. 2402행: "매 응답은 끝에 다음 중 최소 하나를 남긴다: 인물의 요구·부탁·초대·경고, 새로 드러난 정보·소문, 다가온 일정·기한, 눈앞에 벌어진 사건." 2403행: "인물은 먼저 말을 건다 … 화제를 만드는 책임은 플레이어가 아니라 세계에 있다." 반면 2894~2897행 무명 원칙: "카일은 … 기대치 0, 다들 무시하거나 피하는 게 기본값 … 노출 전엔 그 누구도 움직이지 않는다", "★'정체가 뭐지?' 식 의심·추궁·경악·몰려들기 금지 — 세계는 망나니를 수상해할 만큼 그에게 관심이 없다." 여기에 4294행이 매 턴 또 하나를 요구한다: "이 목록에 2명 이상 있으면 매 응답에 최소 1명의 '주인공이 지금 상대하는 인물 외' 다른 인물이 실제 dialogue나 뚜렷한 행동으로 반응해야 한다." 2401행은 반대로 "주인공과 **무관한** 움직임"을 요구한다 — 4294행(주인공 상황에 반응)과 2401행(주인공과 무관)은 같은 턴 슬롯에 대한 상반된 주문이다. 2291~2296행 우선순위 규칙이 헌법 우선이라고 선언하지만, 2399행 헤더가 "(매 턴 적용, 위반 금지)"라 모델이 매번 저울질해야 한다.
- **수정**: 2402·2403행에 헌법 우선 단서를 명시한다 — "단 스토리 헌법이 주인공에 대한 무관심을 기본값으로 규정한 경우, 이 요구는 **주인공을 향한 것이 아니어도 된다**(다른 인물끼리의 용무·옆자리 사건·복도 소문으로 충족)." 그리고 4294행의 '주인공 상황에 반응' 의무와 2401행의 '주인공과 무관한 움직임' 의무 중 하나만 남긴다.

### 되감기(restoreSnap)가 마지막 대사를 dialogueLog에 중복으로 다시 기록한다 — v306 '수신 시점 기록'과 직접 충돌

- **위치**: restoreSnap() 말미 ~15429-15436 (showDialogue 호출) → showDialogue() 15184-15190 → enqueueLine() 10127-10134
- **증상**: 되감기를 한 번 할 때마다 백로그·◀기록넘김에 그 지점 마지막 대사가 한 줄씩 늘어난다(2회 되감기 = 같은 대사 3줄). 세이브에 영구 저장되고, 이후 이어하기의 _replayFill 컷(마지막 유저 입력 이후 삭제)에도 이 줄은 유저 입력 앞에 있어 회수되지 않는다.
- **근거**: restoreSnap은 화면 재구성용으로 `showDialogue([{ speaker: last.speaker, text: last.text, char_id: last.char_id || null }])`를 부른다. 이 객체는 dialogueLog에서 값만 복사한 **새 객체**라 `_log`가 없다. showDialogue는 `(dialogues||[]).forEach(enqueueLine)`뿐이고, enqueueLine의 기록 조건은 `if (d && !d._log && !replaying && !d.isPlayerLine)`이다. restoreSnap 경로에는 `replaying = true` 설정이 없고(파일 전체에서 replaying 대입은 10181 선언·10497 restoreAndReplay·12341 재생완료 3곳뿐, 되감기는 입력모드에서만 진입 가능하므로 그 시점 replaying=false), isPlayerLine도 없다 → **조건이 전부 통과해 dialogueLog.push가 실행된다.** _turnLineKeys 가드도 못 막는다: restoreSnap은 dialogueLog를 `sn.dL`(그 입력 직전 길이)로 자르므로 `last`는 **이전 턴**의 마지막 줄인데, _turnLineKeys에는 방금 되돌린 **현재 턴**의 키만 들어 있다. 표시 시점 `_lastShownKey` 가드도 마지막 표시줄과 다르므로 통과. 그리고 restoreSnap 마지막 줄이 `autoSave()`라 중복분이 세이브에 확정된다.
- **수정**: 표시 전용 재생임을 enqueueLine이 알 수 있게 한다. 가장 작은 수정: restoreSnap에서 새 객체 대신 기록 항목 자체를 참조로 넘긴다 — `const o = { speaker:last.speaker, text:last.text, char_id:last.char_id||null }; Object.defineProperty(o,'_log',{value:last,enumerable:false,writable:true,configurable:true}); showDialogue([o]);` (그러면 showNextDialogue의 `d._log` 갱신도 원본 항목에 정상 반영된다). 또는 showDialogue에 `{noLog:true}` 옵션을 추가하고 enqueueLine 기록 조건에 `&& !d._noLog`를 넣는다.

### 기록 모드·백로그가 '아직 재생되지 않은' 대사를 미리 보여준다 (v306 수신 시점 기록의 부작용)

- **위치**: enqueueLine 10127-10134(수신 시점 push) ↔ historyForward 12718-12729 / renderHistoryEntry 12731-12745 / renderBacklog
- **증상**: 스트리밍 중 기록을 넘기면 아직 오지 않은 대사를 스포일러로 먼저 읽고, 기록에서 빠져나온 뒤 같은 대사가 처음부터 다시 재생된다(같은 내용을 두 번 접함). 미재생 항목은 이전 장면의 스프라이트/배경으로 그려져 '기록 화면이 이상하다'는 증상도 같이 난다.
- **근거**: v306 이후 dialogueLog는 '재생된 만큼'이 아니라 '수신한 만큼' 쌓인다. 반면 historyForward의 상한은 `historyPos < dialogueLog.length - 1`로 **현재 배열 길이**다. 스트리밍 중 유저가 좌측 탭존으로 기록 모드에 들어가면 onChunk(11424~)가 계속 enqueueLine을 호출해 dialogueLog가 실시간으로 늘어나고, 그 새 항목은 아직 화면에 안 나온 대사다. 게다가 그 항목의 `bgFile: currentBgFile, sprites: [...shownSprites]`는 **수신 시점 스냅샷**(비스트리밍 경로는 resetSprites()가 enqueue 이후 12119에 있어 직전 장면 스프라이트)이라 화면 상태도 실제 재생 때와 다르다. 기록 모드 홀드 경로(processResponse 12104-12110)도 동일 — _pendingDialogues로 홀드된 대사가 이미 기록엔 들어가 있다.
- **수정**: 기록 항목에 미재생 표시를 단다: enqueueLine의 L에 `pending: true`를 넣고 showNextDialogue에서 `d._log.pending = false`로 해제. historyForward의 상한과 renderBacklog 렌더 범위를 `첫 pending 항목 직전`까지로 제한한다(경계 계산은 dialogueLog.findIndex(e=>e.pending) 한 줄).

### 요약 트리거 조건이 자기모순 — 히스토리가 120k자를 넘기 전까지 요약도 연대기도 영원히 실행되지 않는다

- **위치**: summarizeIfNeeded() 10763~10766, recentWindowCount() 2188~2199, SUMMARY_INTERVAL=8 (2167), RECENT_CHARS_CAP=120000 (2186)
- **증상**: storySummary와 gameState.chronicle이 초반 ~70턴 동안 완전히 비어 있다. 위 결함1과 합치면 그 구간의 장기 기억 장치는 '원문 히스토리' 하나뿐이고, 원문 창이 밀리는 순간 그 앞 사실은 어디에도 남지 않는다. 유저가 겪는 '오래 하면 앞 얘기를 잊는다'의 구조적 원인.
- **근거**: 게이트는 `if (gameHistory.length - summaryIndex < SUMMARY_INTERVAL + recentWindowCount()) return;`이다. recentWindowCount()는 뒤에서부터 글자수를 누적해 120,000자를 넘을 때까지 count++ 하므로, 총 원문이 120k자 미만이면 count == gameHistory.length가 된다. summaryIndex=0인 초기 상태에서 조건은 `L < 8 + L` = 항상 참 → 무조건 return. 즉 첫 요약은 gameHistory 원문 총량이 120k자를 넘고 항목 수가 약 141개(≈70턴)가 된 뒤에야 일어난다.
- **수정**: 요약 트리거를 창 크기와 분리한다. 예: `unsummarized >= SUMMARY_INTERVAL` (원문 창과 무관하게 8항목마다) 이고 `toSummarize`는 `slice(summaryIndex, length - min(_win, 안전상한))`로 잡되, 요약이 원문을 '대체'하지 않고 '누적 보존'만 하도록 한다(원문 창은 그대로 두고 summaryIndex만 전진). 최소한 첫 요약은 turnCount 기준(예: 10턴)으로도 한 번 발화시켜 chronicle 시드를 만든다.

### trimHistory가 summaryIndex에 묶여 있어 HISTORY_MAX(60)가 사실상 무효 — 세이브가 무한히 자란다

- **위치**: trimHistory() 10202~10211, autoSave() 10214~10216, 주석 10188~10192·10436~10442
- **증상**: 세이브의 gameHistory가 120k자 안팎(≈133항목)까지 자라 유저가 보고한 200~330KB의 대부분을 차지한다. 동시에 매 턴 그 원문 전부를 다시 전송하므로 prefill 지연이 계속 길다. '용량 방어'로 넣은 상한이 실제로는 작동하지 않는다.
- **근거**: trimHistory는 `drop = Math.min(over, Math.max(0, summaryIndex))`로 요약이 끝난 구간에서만 버린다. 결함2로 summaryIndex는 ~70턴까지 0이므로 drop=0 → autoSave의 `if (gameHistory.length > HISTORY_MAX) trimHistory(HISTORY_MAX)`는 아무것도 하지 않는다. 요약이 시작된 뒤에도 trimHistory 직후 summaryIndex가 0으로 되돌아가므로(10209) 실효 상한은 60이 아니라 '120,000자'다. 코드 주석은 여전히 'AI에게 실제로 보내는 건 최근 8개뿐'(10190, 10437)이라고 적혀 있는데 recentWindowCount()는 130개 이상을 반환한다 — 트리밍 설계의 전제가 이미 무너졌다.
- **수정**: HISTORY_MAX를 실효 상한으로 되살리려면 trimHistory의 안전 하한을 summaryIndex가 아니라 `현재 recentWindowCount()`로 잡아야 한다(창 밖은 요약 여부와 무관하게 버려도 프롬프트에 영향 없음 — 단 버리기 전에 반드시 요약/연대기로 흡수). 최소한 주석의 '최근 8개' 전제를 실제 값으로 고치고, RECENT_CHARS_CAP과 HISTORY_MAX 중 어느 쪽이 진짜 상한인지 한 곳으로 통일할 것.

### 슬롯 불러오기가 '이전 플레이의 summaryIndex'로 새 세이브의 히스토리를 잘라낸다 — 요약 경계가 깨지고 원문이 영구 소실

- **위치**: restoreAndReplay() 10436~10442에서 trimHistory 호출, summaryIndex 복원은 10457. 호출처 loadFromSlot() 14713~14720
- **증상**: 70턴 이상 플레이한 뒤 슬롯을 불러오면, 불러온 세이브의 히스토리 앞부분이 최대 (현재 판 summaryIndex)개만큼 조용히 삭제되고, 그 뒤 summaryIndex는 슬롯의 (더 작은) 값으로 덮인다. 삭제된 턴은 원문에도 없고 그 세이브의 storySummary에도 안 들어 있다 — 어디에도 없는 구간이 생긴다. 직후 autoSave(14720)로 손상 상태가 확정 저장된다. 이후 요약도 잘못된 경계에서 시작해 이미 요약된 구간을 다시 요약하거나 건너뛴다.
- **근거**: restoreAndReplay는 `gameHistory = save.gameHistory`(10429) 직후 `if (gameHistory.length > HISTORY_MAX) trimHistory(HISTORY_MAX)`(10436~10438)를 실행하는데, trimHistory가 참조하는 summaryIndex는 이 시점에 **아직 이전 세션/이전 스토리의 전역값**이다. save.summaryIndex 대입은 19줄 뒤인 10457이다. loadFromSlot(14719)은 플레이 도중 호출되므로 현재 판의 summaryIndex(요약이 한 번이라도 돌았으면 >0)가 그대로 적용된다.
- **수정**: restoreAndReplay 안에서 save의 스칼라들(summaryIndex, storySummary, turnCount, histDropped 등)을 **전부 먼저 복원**한 뒤에 트리밍을 수행하도록 순서를 바꾼다. 최소 수정: 10436의 트림 블록을 10457(summaryIndex 대입) 아래로 이동.

### storySummary는 매 요약마다 전체가 통째로 덮어써지고, Gemini 경로는 출력 상한이 요구 분량보다 작아 잘린 요약이 그대로 확정된다

- **위치**: summarizeIfNeeded() 10783(sysText '1000자 이내'), 10820(maxOutputTokens: 600), 10827(storySummary = summary.trim())
- **증상**: 요약이 문장 중간에서 잘리면 그 뒤에 있던 사실 항목(플레이어가 정한 설정·약속·고유명사)이 통째로 사라지고, 다음 요약이 그 잘린 텍스트를 기반으로 다시 압축하면서 영구 소실된다. 스트림이 중간에 끊긴 회차에도 부분 텍스트가 정상 요약으로 확정된다.
- **근거**: 시스템 문구는 '1000자 이내'를 요구하는데 Gemini 직접 호출 경로의 generationConfig는 maxOutputTokens: 600이다(한국어는 글자당 1토큰 이상이라 1000자를 담을 수 없다). 그리고 커밋 조건은 `if (summary && summary.trim())` 뿐 — 길이·형식 검증이 없고, 이전 storySummary를 보존하지 않은 채 통째로 교체한다(10827). 요약 입력에는 `기존 요약`이 포함되므로, 잘린 요약은 다음 회차의 유일한 과거 소스가 되어 손실이 복리로 누적된다. 실패는 `catch {}`로 완전히 침묵한다(10846).
- **수정**: ①Gemini 경로 maxOutputTokens를 1500~2000으로 올리고 finishReason을 확인해 'length'면 커밋하지 않는다. ②커밋 전 sanity check: 새 요약이 기존 요약의 60% 미만 길이거나 마지막 문자가 문장 종결이 아니면 기존 요약을 유지하고 summaryIndex도 전진시키지 않는다. ③직전 요약 1개를 gameState에 백업해 롤백 가능하게 한다.

### IndexedDB 쓰기가 검증 없는 fire-and-forget — 저장 실패가 완전히 은폐되고, autoSave의 용량 회복 사다리는 정상 모드에서 죽은 코드다

- **위치**: _bulkPutIDB() 8279, bulkSetSilent() 8318~8321, autoSave() 폴백 루프 10240~10256
- **증상**: 정상(IDB) 모드: 쿼터 초과·트랜잭션 중단이 나도 앱은 '저장 성공'으로 알고 계속 진행한다. 세션 중에는 메모리 맵이 정답을 돌려주므로 아무 이상이 없어 보이고, 새로고침한 뒤에야 진행이 통째로 사라진 걸 알게 된다. 폴백(iOS PWA 워치독·프라이빗 모드) 모드: 긴 플레이 세이브는 회복이 구조적으로 불가능해 매 턴 토스트만 뜨고 저장이 영구 중단되며, 그 상태에서 클라우드 백업까지 함께 멈춘다.
- **근거**: `_bulkPutIDB`는 `try { _bulkTx('readwrite').put(v,k); } catch {}` 뿐이고 request.onerror/transaction.onabort를 달지 않는다. IDB 모드에서 bulkSetSilent은 메모리 맵 _bulk에 값을 넣고 **무조건 true**를 반환한다(8320). 따라서 autoSave의 try/catch 폴백(스냅샷 삭제 → 히스토리·백로그 축소 재시도)은 _bulkDB가 null인 localStorage 폴백 모드에서만 도달한다. 그 폴백 모드에서도 회복 루프는 `trimHistory(cut)`을 호출하는데 drop이 summaryIndex(대개 0)로 제한되어 0을 반환하므로, 용량의 대부분인 gameHistory를 **한 항목도 줄이지 못한다** — 줄일 수 있는 건 dialogueLog뿐이고 그게 60 이하면 `break`로 빠져나가 저장을 포기한다. 포기 경로는 `return`이라 그 뒤의 persistCfg()와 scheduleAutoBackup()(클라우드 백업)도 건너뛴다(10256~10258, 10266).
- **수정**: ①_bulkPutIDB를 Promise화해 request.onerror/tx.onabort를 잡고 bulkSetSilent이 실제 결과를 반환하게 한다(최소한 실패 시 콘솔+토스트+백업 트리거). ②autoSave 회복 루프의 축소 대상에 gameHistory를 실제로 포함시킨다(summaryIndex 제약을 받지 않는 강제 슬라이스 경로를 따로 두되, 버리기 전에 요약/연대기로 흡수). ③저장 실패 시 return 전에 scheduleAutoBackup()은 반드시 호출한다.

### 이벤트를 '프롬프트에 실은 순간' 발동 처리 — 모델이 무시하거나 에러·재생성이 나면 그 사건은 영구 소실

- **위치**: buildStatePrompt() 4666~4685 (firedEvents.push / _lastEventTurn 기록) / dueEventNow() 4463~4478 / regenerate() 15183~15204 / callGemini catch 11661
- **증상**: 아카데미 이벤트 9개가 '한 번 스쳐 지나가면 끝'인 일회용이 된다. 서열전·본가 소환장·학원제 습격 같은 1부 골격이 모델이 한 번 흘려듣거나 유저가 결과가 마음에 안 들어 재생성을 누른 순간 조용히 사라지고, 그 뒤로는 아무 사건도 오지 않는 '아무 일도 안 일어나는 학원 생활'이 된다. 유저 입장에선 원인이 안 보인다.
- **근거**: 4677~4681에서 프롬프트 문자열을 만든 그 자리에서 바로 `gameState.firedEvents.push(_due.t)`와 `_lastEventTurn = turnCount`를 찍는다. 모델 응답을 보기 전이다. dueEventNow는 4473에서 `!fired.includes(x.e.t)`로 거르고, 4469~4471에서 `tc - _lastEventTurn < EVENT_MIN_GAP(8)`이면 아예 null을 돌려준다. 그래서 (a)모델이 그 턴에 사건을 안 일으켜도 다음 턴엔 지시가 두 번 다시 안 실리고, (b)fetch가 타임아웃·오류로 catch(11661)에 빠져도 이미 발동 처리이며, (c)regenerate()는 gameHistory.pop()과 turnCount-- 만 할 뿐 gameState를 스냅샷으로 되돌리지 않으므로(15188~15195) 재생성한 턴은 사건 지시가 빠진 채로 나간다 — firedEvents에 이미 있고 tc-last가 -1이라 두 필터에 모두 걸린다. 프롬프트 자체는 '이번 턴, 늦어도 다음 한두 턴 안에 실제로 일으켜라'라고 여유를 주는데, 코드는 그 여유를 주지 않는다.
- **수정**: 주입 시점엔 `gameState._pendingEvent = {t, turn}`만 찍고, 커밋은 응답이 성공적으로 파싱된 뒤(processResponse 진입부, !replaying)에 한다. 커밋 조건을 좀 더 엄격히 하려면 응답의 narration+dialogue에 이벤트 키워드가 있는지 보거나, 최소한 '주입 후 2턴 안에 커밋 안 되면 재주입'하도록 _pendingEvent를 재사용한다. regenerate()는 직전 captureSnap의 gameState로 복원한 뒤 재호출하도록 고친다(지금은 친밀도 클램프·metChars 자동등록도 재생성마다 이중 적용된다).

### 연대기 상한 60인데 프롬프트엔 뒤 30줄만 — 저장분 절반은 세이브만 먹고, 잘릴 땐 가장 근본적인 초기 사실부터 사라진다

- **위치**: summarizeIfNeeded 10831 `slice(-60)` / buildStatePrompt 4656~4660 `chron.slice(-30)` / 상태창 15073~15077 `slice(-15)`
- **증상**: 연대기는 buildStatePrompt에서 '이미 확정된 과거 — 절대 모순되게 쓰지 말 것'이라는 최강 앵커로 실리는데, 그 앵커에서 가장 먼저 빠지는 게 세계관의 뿌리다. 장기 플레이에서 '왜 갑자기 주인공 출신을 다르게 말하지'가 여기서 나온다. 게다가 finding 1 때문에 연대기 항목이 쌓이는 속도 자체가 요약 패스 1회당 0~3줄로 느려서, 30줄이 채워질 무렵엔 이미 100턴 이상 진행된 뒤다.
- **근거**: 세 곳의 숫자가 전부 다르다. 저장은 60, 모델에게 보내는 건 30, 유저 화면엔 15. 즉 31~60번째 오래된 항목은 저장·동기화·스냅샷(captureSnap이 gameState 통째를 60개 뜬다, 15244~15252) 용량만 차지하고 모델은 절대 못 본다. 그리고 절단 방식이 `slice(-60)` — 무조건 오래된 것부터 버린다. 연대기 항목의 성격상 오래된 줄일수록 '주인공의 정체', '가문의 진실', '첫 약속'처럼 뒤집히면 이야기가 통째로 무너지는 사실이고, 최근 줄일수록 '오늘 누구와 밥을 먹었다'급이다. 중요도와 정확히 반대 순서로 버린다.
- **수정**: 저장 상한과 노출량을 일치시키고(예: 저장 40 / 노출 40), 절단을 `slice(-N)`에서 '앞 앵커 + 최근'으로 바꾼다: `chronicle = [...chronicle.slice(0, 12), ...chronicle.slice(-28)]`. 더 나은 방법은 항목에 `pin: true` 플래그를 두고(정체·혈통·죽음·유저확정 설정) 핀은 절단 대상에서 제외하는 것. 상태창의 15도 같이 올린다.

### 요약 재압축 손실 + Gemini 경로는 자기 지시(1000자)보다 작은 출력 상한(600 토큰)이라 잘린 요약이 그대로 정본이 된다

- **위치**: summarizeIfNeeded 10761~10763(existingCtx 재투입), 10771(sysText '1000자 이내'), 10806~10814(Gemini generationConfig maxOutputTokens: 600), 10817(storySummary = summary.trim())
- **증상**: 창 밖으로 나간 모든 것의 유일한 운반체가 storySummary인데, 그게 매번 조금씩 짧아지고 뒤가 잘린다. 대개 요약문 뒷부분이 최신 사건이므로 '가장 최근에 창을 벗어난 사건'이 우선 소실된다. 장기 플레이에서 중반부 기억이 통째로 비는 전형적 증상이 여기서 나온다.
- **근거**: 매 패스마다 `기존 요약 + 새 원문`을 통째로 넣고 '하나의 통합 요약'을 1000자 이내로 다시 쓰게 한다. 즉 요약이 요약을 재압축하는 구조라 손실이 누적된다(패스가 8엔트리마다 돌므로 200턴이면 수십 회 재압축). 더 구체적인 결함은 프로바이더별 상한 불일치다: OpenRouter 경로는 max_tokens 1500(gemini-3은 4000)인데, 기본 프로바이더인 Gemini 직행 경로는 maxOutputTokens: 600이다. 한국어에서 600토큰은 1000자에 못 미친다(대략 600~900자). 그리고 코드는 finishReason을 전혀 보지 않고 `data.candidates?.[0]?.content?.parts?.[0]?.text`를 그대로 storySummary에 대입한다 — MAX_TOKENS로 문장 중간에 끊긴 텍스트가 새 정본이 되고, 다음 패스는 그 잘린 텍스트를 '기존 요약'으로 받아 또 압축한다. 시스템 프롬프트가 '★플레이어가 정하거나 정정한 설정은 삭제 금지'라고 애원하는 것도, 출력 상한에 걸려 뒷부분이 잘리면 무의미하다.
- **수정**: Gemini 경로 maxOutputTokens를 1200~1500으로 올리고, 응답의 finishReason이 MAX_TOKENS면 storySummary를 교체하지 말고 기존 것을 유지한다(summaryIndex도 전진시키지 않아 다음 턴에 재시도된다 — 지금도 실패 시 재시도 구조는 맞게 되어 있다). 근본적으로는 산문 요약과 '확정 사실 목록'을 분리해서, 사실 목록은 재압축 대상이 아닌 append-only(chronicle)로만 보존한다.


---

## MEDIUM

### '반 배정'이 최소 간격에 밀려 9턴째에 주입되고, 11턴을 넘기면 영구 폐기된다

- **위치**: novel.html:4470-4477 dueEventNow의 gap·`due<=2 && tc>10` 필터 / 2937 ACADEMY_EVENTS 반 배정(cond '1일차 (입학식 직후)')
- **증상**: 두 갈래 증상이 다 나온다. (a) 모델이 이벤트 트리를 보고 1턴에 이미 반을 정했는데 9턴째에 앱이 '지금 반 배정을 실제로 일으켜라'를 들이밀어 반 선택 choices가 두 번 나온다 — 이미 흑룡반인데 다시 고르라고 한다. (b) 반대로 9~10턴 창을 놓치면 flags['반'] 없이 영원히 무소속으로 진행돼, 반 대항전·홈룸 같은 소속 기반 연출이 통째로 비게 된다.
- **근거**: '입학식 — 세라핀 충돌'과 '반 배정'은 둘 다 due=1이다. 정렬이 안정 정렬이라 입학식이 먼저 나가고 _lastEventTurn이 잡히면, 반 배정은 `tc - last >= 8`을 만족하는 9턴째에야 후보가 된다(4470-4471). 그런데 같은 함수에 `.filter(x => !(x.due <= 2 && tc > 10))`(4475)이 있어 11턴을 넘기면 후보에서 영구 제외된다. 앱은 flags['반']이 이미 찍혔는지 확인하지 않고, 발동 여부 판단에 firedEvents(자기가 주입했는지)만 쓴다.
- **수정**: 주입 전에 이미 성립한 사건을 걸러낸다 — 반 배정은 `gameState.flags['반']`이 있으면 skip하고 firedEvents에 조용히 스탬프. 일반화하려면 이벤트에 `doneWhen`(flags 키/정규식) 필드를 두고 dueEventNow가 먼저 검사하게 한다. 1턴 도래 이벤트는 EVENT_MIN_GAP 면제.

### 백스톱의 슬롯 파싱이 실패하면 시간이 '오후'로 되감긴다 (새벽·괄호 표기)

- **위치**: novel.html:11816-11827 advanceTimeSlotIfStale / 11789 TIME_SLOTS
- **증상**: 밤 장면에서 백스톱이 걸리면 다음날 아침이 아니라 같은 날 '오후'로 시간이 거꾸로 간다. HUD 토스트('🕐 3일차 오후')는 정상처럼 뜨고, 다음 턴 프롬프트에도 오후가 실려 모델이 밤 장면 직후에 오후 장면을 쓴다. 낙인의 검·무협처럼 '1일차 새벽'으로 시작하는 스토리에서는 첫 백스톱이 곧바로 새벽→오후 점프가 된다.
- **근거**: TIME_SLOTS = ['아침','오전','오후','저녁','밤']에는 '새벽'이 없다. 슬롯은 `\s*(\S+)?`로 잘라내므로 공백이 없는 부가표기가 붙으면 통째로 캡처된다 — '3일차 밤(통금 후)'이면 m[2]='밤(통금'이 되고, '3일차, 밤'이면 ',밤'이 된다. 이때 `i = TIME_SLOTS.indexOf(...)`가 -1이 되어 `if (i < 0) i = TIME_SLOTS.indexOf('오전')`로 폴백한 뒤 i++ 되어 '오후'가 최종값이 된다(11823-11826).
- **수정**: 정규식 캡처 대신 `TIME_SLOTS.find(sl => cur.includes(sl))`로 슬롯을 찾고, '새벽·정오·한낮·심야·자정' 별칭 매핑 테이블을 둔다. 그래도 못 찾으면 밀지 말고 null 반환(현재는 임의로 오후로 만든다).

### 요일과 일과가 프롬프트에 전혀 안 실린다 — LORE의 주간 시간표는 모델이 매번 새로 지어낸다

- **위치**: novel.html:2878 ACADEMY_LORE 시간표 / 4611 상태줄 / 4617 academy 전용 주입(메타봉인 1건뿐) / 대조군 4591 ldNowClass
- **증상**: '수요일 마도공학인데 가레스가 들어온다', '3일차 연속으로 검술 수업', '주말인데 정상 수업' 같은 일이 무작위로 난다 — LORE에 '요일에 맞는 교수를 등장시켜라'라고 적어놨지만 모델은 오늘이 무슨 요일인지 알 방법이 없다. 시간이 흘러도 그 시간대에 맞는 일정(오전=이론 통합강의, 오후=연무장 실기, 22시 통금)이 전달되지 않아 저녁·통금 규칙도 지켜지지 않는다. 유저 기준 증상은 '시간표를 하나도 안 지킨다'(연애일기에서 같은 지적을 받고 ldNowClass를 만든 그 문제가 아카데미에는 미이식 상태).
- **근거**: ACADEMY_LORE는 '하루: 아침 홈룸→오전 이론→오후 실기→저녁 자유→22시 통금, 주말 휴강'과 '월 마법이론=모르가나 / 화 검술=가레스 / 수 마도공학=비비안 / 목 전술=가레스 / 금 종합 실기평가'를 고정 규칙으로 명시한다(2878). 그런데 novel.html 전체에서 일차→요일을 계산하는 코드는 없다(grep 요일/weekday 결과: 아카데미 관련 히트 0 — 4128~4217은 전부 lovediary의 ld* 시간표 주입기다). buildStatePrompt가 academy에 전용으로 붙이는 문장은 4617의 메타 봉인 한 줄뿐이고, 시간 정보는 상태줄의 `| 3일차 오후` 문자열 하나가 전부다(4611).
- **수정**: lovediary의 ldNowClass와 같은 academy 전용 주입기를 만든다: day=일차에서 `(day-1)%7`로 요일 산출(6·7=주말 휴강), 슬롯→일과 매핑('오전'→이론 통합강의, '오후'→연무장 실기, '저녁'→자유·22시 통금), 요일→담당 교수를 합쳐 '★지금 4일차(목) 오후 — 전술·대륙사 후 연무장 실기, 담당 가레스' 한 줄을 buildStatePrompt에 싣는다.

### 이벤트 트리 정적 규칙("조건 안 됐으면 억지로 발생시키지 말 것")과 동적 도래 블록("지금 일으켜라")이 서로를 부정한다

- **위치**: loreEndingsEventsBlock 4258행(정적, ACADEMY_WORLD_PROMPT에 상시 포함) vs buildStatePrompt 4670~4677행(동적)
- **증상**: 모델이 정적 규칙 쪽을 따르면 사건을 소문·예고로만 흘리는데, 그 턴에 이미 firedEvents에 기록돼(위 2번 항목) 사건이 영구 소실된다. 두 결함이 곱해져 '사건이 예고만 되고 안 일어남'이라는 정확한 증상을 만든다.
- **근거**: 4258행이 매 턴 시스템 프롬프트에 넣는 헤더: "## 이벤트 트리 (각 이벤트는 발동 조건이 충족되는 시점에 자연스럽게 일으킬 것. **조건이 안 됐으면 억지로 발생시키지 말 것**)" — 그 아래 각 이벤트의 조건은 '입학 1개월 후'처럼 서사 시간으로 적혀 있다(2937~2943행). 같은 프롬프트의 4671~4676행은 정반대로 말한다: "## ★지금 일어날 사건: [신입생 서열전] … 이 사건을 이번 턴, 늦어도 다음 한두 턴 안에 **실제로 일으켜라**. 예고나 소문으로만 흘리고 넘기지 말 것." 15턴째 time이 '2일차'인 상태에서 두 지시를 동시에 받으면, 정적 규칙 기준으로는 '조건 미충족'이고 동적 지시 기준으로는 '지금 당장'이다.
- **수정**: 동적 블록이 뜬 턴에는 정적 이벤트 트리에서 해당 항목을 빼거나, 4258행 헤더를 "단 앱이 '★지금 일어날 사건'을 지정한 턴에는 그 지시가 이 조건보다 우선한다"로 보강한다.

### 출력 JSON 스키마가 제목 없이 산문 규칙 한복판에 박혀 있고, 형식 지시와 40행 떨어져 있다

- **위치**: ENGINE_RULES 2304행(형식 지시) vs 2345행(스키마 시작 `{`)
- **증상**: 스키마가 '지시'가 아니라 '예시 텍스트'로 읽힐 여지가 커진다. 필드 누락(choices·cg·chronicle)이나 마크다운 코드펜스 동봉이 산발적으로 나오는 유형의 결함과 정합한다(로그로 재확인 필요).
- **근거**: 2304행에 "매 응답 시 반드시 JSON 형식으로만 응답할 것 (마크다운 코드블록 없이 순수 JSON)"이 있고, 실제 스키마는 41행 뒤인 2345행에서 아무 헤더 없이 맨 `{`로 시작한다. 그 바로 위 2344행은 "- **턴 마무리는 훅으로**: …"라는 서사 조언 불릿이고, 스키마가 끝나는 2392행 다음은 "## 상태 추적 규칙"이다. 즉 스키마는 '## 서사 몰입 원칙' 섹션의 마지막 항목처럼 보이는 위치에 있으며, "## 출력 형식 (이 JSON만 출력)" 같은 제목이 없다. 두 지점 사이에는 분량 규칙·서사 몰입 원칙 약 1,500자가 끼어 있다.
- **수정**: 2345행 앞에 `## ★★출력 형식 — 반드시 아래 JSON **하나만** 출력한다(코드블록·설명문 금지)` 헤더를 넣고, 스키마 블록을 ENGINE_RULES의 맨 끝(수위 규칙 뒤) 또는 맨 앞으로 옮겨 산문과 섞이지 않게 한다.

### 같은 규칙이 2~5곳에서 미묘하게 다른 말로 반복된다 (중복 규칙 다발)

- **위치**: ENGINE_RULES 2332/2399~2404/2419행, 2342/2414행, 2343/2417행, 2335/2396/2420행, 2333행+11397행+2564행, 2579행+11390행(cgBlock)
- **증상**: 같은 지시가 여러 번 나오면 강조가 아니라 잡음이 된다(⑤처럼 반대로 갈리면 특히). 프롬프트만 무거워지고 어느 판본을 따를지는 모델이 정한다.
- **근거**: 읽은 범위에서 확인된 중복. ①'세계는 스스로 움직인다'가 3곳: 2332행("NPC는 각자의 목적으로 움직인다"), 2399~2404행(v317 섹션 6줄), 2419행("능동적 세계"). ②복선이 2곳: 2342행("복선을 심고 회수하라"), 2414행("복선(체호프의 총)"). ③완급이 2곳으로 리듬 표기가 다름: 2343행 "긴장→이완→더 큰 긴장" vs 2417행 "위기→해소→일상→더 큰 위기". ④비밀 3단계가 4곳: 2335행(Secret), 2396행(비밀 공개 추적), 2420행(GM 전용 정보 취급), ACADEMY_LORE의 GM 전용 비밀 헤더. ⑤이름 창작 금지가 3곳이고 그중 하나는 반대 지시: 2333행(금지), 11397행 systemContent(금지), 2564행 MOD_CG("필요하면 만들되 sim.person으로 등록하라"). ⑥CG 씬키 71개 목록이 2곳: 2579행(MOD_CG)과 11390행 cgBlock(CG_SCENES_BASE+EXTRA, 13092~13093행) — 약 1,200자 그대로 중복.
- **수정**: 각 주제를 한 문장으로 통합한다: ①은 v317 섹션 하나로 흡수하고 2332·2419행 삭제, ②③은 하나씩 삭제, ④는 2420행만 남기고 2335·2396행은 '자세한 것은 GM 전용 정보 취급 참조'로 축약, ⑤는 MOD_CG 오배송 수정(6번 항목)으로 자동 해소, ⑥은 2579행 목록을 지우고 런타임 cgBlock만 남긴다.

### 정적 규칙 총량 약 77,000자 + 매 턴 '반드시' 의무 15개 이상 — 지시 준수를 떨어뜨릴 수준으로 판단된다

- **위치**: ENGINE_RULES 2290~2433행 / ENGINE_MOD_RPG 2436~2451행 / ENGINE_MOD_CG 2452~2588행 / ACADEMY_LORE 2851~2934행 / callGemini 11396~11401행
- **증상**: 턴마다 어떤 의무는 지켜지고 어떤 의무는 조용히 탈락한다. 어느 것이 탈락할지는 예측 불가라, 규칙을 더 추가해도 개선이 안 되고(최근 v305~v318이 전부 '규칙 추가' 방식이었다) 오히려 기존 규칙의 준수율을 떨어뜨린다. "규칙을 넣었는데 안 지킨다"는 관찰의 구조적 원인으로 보인다.
- **근거**: 실측 문자 수(sed+wc -m): ENGINE_RULES_PROMPT 27,982자(과제문에 적힌 '약 13,000자'의 2배 이상 — 스키마 포함), ENGINE_MOD_CG 20,178자, ACADEMY_LORE 16,571자, ENGINE_MOD_RPG 4,672자, ACADEMY_EVENTS+ENDINGS 약 4,300자. playerBlock·cgBlock·roster·bg 블록까지 합치면 아카데미 시스템 프롬프트는 약 77,000자다. 코드 자체의 환산 기준(2185행 주석 "한글 기준 1자 ≈ 1토큰(보수적)")을 쓰면 그대로 토큰 수에 해당한다. 여기에 동적 dynamicCtx(charPrompt + buildStatePrompt) 약 5,000~9,000자와 히스토리 최대 120,000자(RECENT_CHARS_CAP, 2187행)가 더 붙는다. 그리고 '매 턴/매 응답 반드시'로 표기된 의무를 세어 보면 최소 15개다 — 분량 650~900자(2317행), narration 2~3개+대사 4~7개(2318행), choices 최소 2개(2325행), 긴장 하나(2331행), 주인공과 무관한 움직임 하나(2401행), 응답 끝의 요구/정보/일정/사건(2402행), 훅으로 마무리(2344행), 다른 인물 1명 반응(4294행), 전투 판정 표기(2446행), chronicle 기록(2391행), cg 갱신(2380행), time 갱신(2378행), 감정 관성(2339행), 도래 사건 발동(4672행), pendingBeat 1회 의무(4560행). 이 중 일부는 서로 배타적이다(1번·7번 항목).
- **수정**: ①즉시 효과: MOD_CG 오배송 제거(-17,904자, 약 23%). ②중복 규칙 통합(10번 항목, 추가 -3,000자 내외). ③'매 턴 의무'를 5개 이하로 줄이고 나머지는 '권장'으로 강등하거나, 조건부 의무는 buildStatePrompt에서 해당 턴에만 주입한다(이미 메타 봉인 교정약 4610행이 쓰는 방식 — 상시 비용화하지 않는 좋은 선례다). ④캐싱 관점에서는 현재 static-first/dynamic-last 배치(11396~11404행)가 이미 옳다 — 문제는 캐싱이 아니라 '정적 영역에 무관한 규칙이 섞여 있는 것'이다.

### _isNarrationEcho의 '문장 단위' 확대(v305)가 정당한 캐릭터 대사를 통째로 삼킨다 — 화자·스프라이트 소실

- **위치**: _isNarrationEcho 10160-10169, 사용처: onChunk 11430-11431·11470·11484, processResponse 12084-12086·12052-12053
- **증상**: 인물이 말하는데 이름표가 '나레이터'로 뜨고 스프라이트가 안 나타난다(ensureSprite는 dialogue의 char_id로만 호출됨). v317에서 NPC 능동성 규칙을 넣어 NPC 대사량이 늘수록 이 경로를 타는 빈도가 올라간다.
- **근거**: _echoNorm(10156)은 따옴표·괄호·구두점을 전부 제거한다. _isNarrationEcho는 narration을 `split(/(?<=[.!?…。])\s+|
+/)`로 쪼갠 뒤 각 문장과 dialogue.text를 _sameText로 비교해 하나라도 같으면 dialogue 쪽을 버린다. 모델이 흔히 쓰는 형식 — narration에 「"나는 그걸 믿지 않아."」 같은 인용 문장을 독립 문장으로 넣고 dialogue 배열에도 같은 대사를 넣는 경우 — 에서 따옴표가 지워져 정규화 결과가 정확히 일치하므로 **dialogue 항목이 삭제된다**. 남는 건 나레이터 발화뿐이다(char_id·emotion·pose 없음). 임계는 정규화 6자 이상이라 한국어 한 문장이면 대부분 걸린다.
- **수정**: 버리는 쪽을 뒤집는다. dialogue 항목을 살리고 narration에서 그 문장을 제거하도록 바꾼다(정보량: dialogue > narration — 화자·표정·포즈를 갖고 있다). 스트리밍 경로는 narration이 이미 방출된 뒤일 수 있으므로, 최소한 '문장 전체 일치 + 그 문장이 따옴표로 감싸여 있던 경우'로 한정해 현재 필터를 좁히는 것도 대안.

### showDialogue의 큐 비우기와 _pendingDialogues 덮어쓰기는 dropLogEntry로 회수되지 않는다 — 화면엔 없고 기록엔 있는 유일한 구조적 구멍

- **위치**: showDialogue 15184-15190, processResponse 기록모드 홀드 12104-12110, dropLogEntry 10171-10175
- **증상**: 유저가 읽은 적 없는 대사가 기록/백로그에 존재한다(v306이 없애려던 불일치의 반대 방향). 재생은 영영 안 되므로 '대사가 통째로 안 나왔는데 기록엔 있다'로 보인다.
- **근거**: dropLogEntry는 showNextDialogue의 3개 표시 취소 지점(빈 텍스트 12363, 유저 에코 12370-12371, 연속 중복 12375)에서만 호출된다. 반면 showDialogue는 첫 줄이 `dialogueQueue = []`이고, historyMode면 `window._pendingDialogues = dialogues; return;`으로 **기존 _pendingDialogues를 통째로 덮어쓴다**. 두 경로 모두 버려지는 항목들의 `_log`를 회수하지 않는다. 구체 시나리오: (1) 기록 모드 열람 중 응답 도착 → processResponse가 큐를 _pendingDialogues로 홀드(대사는 이미 수신 시점에 dialogueLog에 기록됨) → (2) 그 상태에서 오류/시스템 메시지가 showDialogue로 들어옴(11689 catch의 '오류: ', 11553 429 안내 등) → _pendingDialogues가 시스템 한 줄로 덮어써짐 → (3) 그 턴 대사는 화면에 한 줄도 재생되지 않지만 dialogueLog와 세이브에는 전부 남는다.
- **수정**: 큐를 버리는 모든 지점에서 회수한다. `const flushQueue = () => { dialogueQueue.forEach(dropLogEntry); dialogueQueue = []; };`를 만들어 showDialogue와 스토리 초기화(10401)에서 사용하고, historyMode 홀드에서는 덮어쓰기 대신 `window._pendingDialogues = (window._pendingDialogues||[]).concat(dialogues)`로 이어붙인다.

### _turnLineKeys 차단 임계가 화자 이름 길이에 좌우된다 + showDialogue 계열 턴은 resetTurnLineKeys를 안 부른다

- **위치**: enqueueLine 10115-10125(`if (_tk.length > 11)`), resetTurnLineKeys 호출: 11463(스트림 활성)·12072(비스트리밍)뿐
- **증상**: 같은 턴 안에서 의도된 반복(구호·후렴·되뇌는 대사)이 화자에 따라 조용히 사라진다. 그리고 직전 턴과 같은 문구의 시스템/안내 라인은 물려받은 키에 걸려 화면에 아예 안 뜬다(추론 — 실측 아님).
- **근거**: 키는 `speaker + '|' + _echoNorm(text)`이고 길이 11 초과일 때만 차단 대상이다. 화자 이름이 키에 포함되므로 임계가 인물마다 다르다 — 나레이터(4자)+'|'=5 → 본문 7자부터 차단, '릴리아'(3자)+'|'=4 → 본문 8자부터, 이름이 긴 인물은 본문 4~5자부터 차단된다. 즉 '정당한 짧은 반복은 봐준다'는 설계 의도가 화자에 따라 들쭉날쭉하다. 또 resetTurnLineKeys는 AI 턴 진입 2곳에만 있어, showDialogue로 재생되는 턴(시스템/오류 메시지, exitHistory의 보류분, restoreSnap 재구성)은 **직전 AI 턴의 키 집합을 그대로 물려받은 채** enqueueLine을 탄다.
- **수정**: 임계를 화자 제외 본문 기준으로 바꾼다(`if (_echoNorm(d.text).length > 8)`처럼 본문 길이만으로 판정). resetTurnLineKeys를 '턴 경계' 단일 지점으로 올린다 — callGemini 진입부와 renderFixedStep 진입부에서 부르고, showDialogue 계열은 애초에 키 검사 대상에서 빼거나(표시 전용 플래그) 호출 전에 리셋한다.

### _lastShownKey가 어디에서도 리셋되지 않는다 — 턴/세이브 경계를 넘어 정당한 대사를 삭제하며, _replayFill 리필과 겹치면 기록 영구 소실

- **위치**: showNextDialogue 12375-12376 (설정만 있고 초기화 없음), restoreAndReplay 10489-10499(_replayFill 컷), 스토리 초기화 10401-10403
- **증상**: ① 응답 폴백 라인('(잠시 정적이 흐른다...)' 11662)처럼 앱이 만드는 고정 문구가 두 턴 연속 나오면 두 번째 턴은 화면에 아무것도 안 나온 채 선택지로 넘어간다. ② 같은 세이브를 슬롯에서 다시 불러오면 마지막 응답의 첫 줄(응답이 한 줄이면 그 턴 전체)이 기록에서 영구히 사라진 채 저장된다.
- **근거**: grep 결과 `_lastShownKey`는 12375(비교)·12376(대입) 두 곳뿐이고 리셋이 없다. 스토리 전환 청소 블록(10401-10403)이 _genText·_pendingDialogues·_present는 지우면서 이건 안 지운다. 따라서 새 턴의 첫 줄이 직전 턴 마지막 줄과 같으면 표시·기록 모두에서 삭제된다(dropLogEntry 동반). 특히 위험한 조합: 세이브 슬롯 로드(14719 restoreAndReplay)는 `_lastShownKey`가 현재 세션 값인 채로 시작하는데, restoreAndReplay는 마지막 유저 입력 이후 기록을 **먼저 잘라내고**(10494) _replayFill로 재생분만 다시 채운다. 잘라낸 줄이 stale _lastShownKey에 걸려 표시 스킵되면 **컷은 됐는데 리필은 안 된 상태**가 되고, 재생 완료 시점의 autoSave(12341-12343)가 그 상태를 확정한다.
- **수정**: 턴 경계와 복원 경계에서 초기화한다 — resetTurnLineKeys() 안에 `window._lastShownKey = null;`을 같이 넣고, restoreAndReplay 진입부(10426 직후)와 스토리 초기화 블록(10401)에도 추가한다.

### 연대기 추출이 요약 구간의 마지막 4000자만 본다 — 요약된 턴 대부분의 사건이 연대기에 들어가지 않는다

- **위치**: summarizeIfNeeded() 10834 `newContent.slice(-4000)`, 상한 10841
- **증상**: 연대기는 buildStatePrompt에서 '절대 모순되게 쓰지 말 것'으로 주입되는 유일한 하드 앵커인데, 그 앵커가 각 요약 주기의 마지막 1~2턴 분량만 담는다. 요약 산문은 다음 회차에 다시 압축되며 흐려지므로, 결국 대부분의 확정 사실은 어느 층에도 남지 않는다.
- **근거**: 연대기 추출 llmSmall 호출은 `newContent.slice(-4000)`을 입력으로 준다. newContent는 이번에 요약되는 전체 구간(첫 실행 시 10여 항목 ≈ 15,000~25,000자, 이후에도 8항목 ≈ 8,000~15,000자)인데, 뒤 4,000자만 잘라 넣는다. 앞부분에서 일어난 사건은 요약 산문에는 남을 수 있어도 연대기 항목으로는 추출되지 않는다.
- **수정**: newContent 전체를 넣되 4000자 초과 시 앞/뒤로 쪼개 2회 호출해 결과를 합치거나, 애초에 결함1(턴별 chronicle 수집)을 고쳐 요약 패스는 보조 수단으로만 쓴다.

### 연대기는 60개 저장하고 30개만 주입 — 잘리는 쪽이 하필 원문이 이미 사라진 '가장 오래된 확정 사실'이다

- **위치**: 저장 상한 summarizeIfNeeded 10841 `slice(-60)`, 주입 buildStatePrompt 4663~4667 `chron.slice(-30)`
- **증상**: 보관 중인 연대기의 절반이 모델에게 한 번도 전달되지 않는다. 더 나쁜 건 절단 방향이다 — 오래된 항목일수록 원문 히스토리에서 이미 밀려난 사실이라 연대기가 유일한 근거인데, 그게 먼저 잘린다. '초반에 정한 설정이 후반에 뒤집힌다'가 정확히 이 형태로 나온다.
- **근거**: 저장은 최근 60개 유지, 프롬프트 주입은 `chron.slice(-30)`으로 최근 30개만. 두 곳 모두 최신 우선 절단이다. push 시 중복 검사도 없어(10840) 같은 사실이 여러 번 쌓일 수 있고, 그만큼 유효 슬롯이 줄어든다.
- **수정**: 주입 상한을 저장 상한과 맞추거나(30 → 60), 절단을 '최근 N + 초반 고정 M(플레이어가 정한 설정·죽음·약속 등 앵커)' 방식으로 바꾼다. push 시 정규화 문자열 기준 중복 제거 추가.

### metChars 블록이 무제한·무우선순위로 매 턴 전량 주입 — 인원이 늘수록 커지고, 정작 중요한 연대기보다 자리를 많이 차지한다

- **위치**: buildStatePrompt() 4638~4661(전량 forEach), 4522~4536(단계별 중복 나열), 자동 등록 processResponse 12026~12029
- **증상**: 지금 장면에 없는 인물, 30턴 전에 한 번 스친 인물, 죽은 인물이 모두 '지금 방에 있는 사람'과 동일한 지면을 차지한다. 반대로 실제 장기 서사를 지탱해야 할 연대기는 30줄로 제한된다 — 우선순위가 뒤집혀 있다. 인물이 많은 스토리일수록 상태 프롬프트가 커져 지연도 함께 는다.
- **근거**: `ids.forEach`로 metChars 전원을 상한 없이 출력한다. 항목당 이름/관계/친밀도/상태/경지/노트 한 줄 + 관계 단계 설명 문단(80~110자)이 붙어 약 180~250자다. 게다가 같은 인물의 친밀도가 4522~4536의 '단계가 오른 사람'/'아직 남인 사람' 줄에 한 번 더 나열된다. 사망 인물도 '[사망—재등장 금지]' 태그를 달고 영구히 남는다. 인물은 대사만 해도 자동 등록된다(12026: char_id가 있으면 metChars에 추가). 아카데미 로스터는 약 24명(2822~2847)이라 만난 인원이 다 차면 5~6KB가 매 턴 고정 비용으로 실린다.
- **수정**: metChars 주입을 계층화한다: ①이번 장면 등장(_lastPresentIds)은 전체 필드, ②최근 N턴 내 등장은 이름·관계·친밀도만, ③그 외/사망자는 '이름(사망)' 한 줄 요약 목록으로. 4522~4536의 중복 나열은 ①/②에 이미 포함되므로 제거하거나 그 계층에서만 출력.

### 인물별 지식(knows) 시스템이 실질적으로 죽어 있다 — 프롬프트는 읽지만 아무도 쓰지 않고, 불러오기마다 삭제된다

- **위치**: 읽기 buildStatePrompt 4658, 삭제 restoreAndReplay 10455, 스키마 chars 정의 2369(knows 없음)
- **증상**: '누가 무엇을 알고 있는가'를 기억하는 장치가 존재하지 않는다. 비밀이 누구에게 들켰는지, 누가 아직 모르는지가 상태로 남지 않으므로 모델은 원문 창이 밀리는 순간 전원이 다 아는 것처럼(혹은 아무도 모르는 것처럼) 굴게 된다. 결함1(chronicle 폐기)과 겹쳐 '비밀 공개' 계열 서사가 가장 먼저 무너진다.
- **근거**: 4658은 `if (m.knows) s += ... 지식: ... (모르는 정보는 절대 아는 것처럼 말하지 말 것)`으로 극적 아이러니를 주입한다. 그러나 응답 스키마의 chars 객체 필드는 relationship/intimacy/condition/note/location/tier/allegiance뿐이고 knows가 없다(2369). processResponse의 metChars 병합(11957~11975)도 knows를 쓰지 않는다. 게다가 restoreAndReplay는 로드할 때마다 `delete _mc[_id].knows`를 실행한다(10455, 폐지된 감독 잔재 청소 목적). 파일 전체에서 knows는 이 두 곳뿐이다.
- **수정**: knows를 되살릴 거면 스키마 chars에 필드를 추가하고 병합 코드에 넣고 10455의 delete에서 knows만 빼야 한다(arc 삭제는 유지). 되살릴 생각이 없으면 4658의 주입 코드를 제거해 죽은 경로를 정리한다. 둘 중 하나로 확정할 것 — 지금은 '있는 척하는 기능'이다.

### 되감기 스냅샷 60개가 각각 gameState 전체(연대기·metChars)와 요약을 통째로 복사해 저장 — 세이브보다 큰 숨은 용량

- **위치**: captureSnap() 15268~15302 (s: JSON.stringify({gameState, storySummary, ...})), SNAP_MAX=60 (15261), persistSnaps 15305~15325
- **증상**: 실제 스토리당 저장 소비가 체감보다 2~3배 크다. localStorage 폴백 모드에서는 이 키가 먼저 5MB를 잠식해 세이브 자체를 밀어낸다(persistSnaps는 자기 키만 줄이고 세이브 실패는 별도 경로라 서로 조율되지 않는다). 결함6의 회복 불가 상태에 더 빨리 도달한다.
- **근거**: 스냅샷 본문에서 gameHistory·dialogueLog는 길이만 남기도록 이미 최적화됐지만(hL/dL), `s` 안에는 gameState 전체가 직렬화된다. gameState에는 metChars 24명(≈5KB)과 chronicle 최대 60줄, storySummary(1~2KB)가 들어간다. 즉 스냅샷 1개가 대략 8~12KB이고 60개면 500KB~700KB로, 유저가 말한 세이브 200~330KB보다 크다. 별도 키(vn_snaps_<story>)에 저장되므로 세이브 용량 표시에는 안 잡힌다.
- **수정**: 스냅샷의 s에서 storySummary를 빼고(요약은 되감기 시 근사 복원이 가능하거나, 요약 변경 지점에만 저장), chronicle은 전량 복사 대신 그 시점 길이만 기록해 자르기 방식으로 복원한다(gameHistory와 동일한 hL/dL 기법). 또는 SNAP_MAX를 20~30으로 낮춘다.

### metChars 프롬프트 블록에 상한이 없다 — 만난 인물이 늘수록 매 턴 고정비가 선형으로 커지고, 한 번 스친 인물도 영구히 남는다

- **위치**: buildStatePrompt 4631~4655(전원 순회 + 관계 단계 문장) / 4514~4530(단계가 오른 사람 · 아직 남인 사람 블록) / processResponse 11992~11995(발화만 해도 자동 등록)
- **증상**: 토큰 비용도 문제지만 더 큰 건 주의 희석이다 — 지금 눈앞에 있는 2명과 30턴 전에 한마디 하고 사라진 22명이 같은 형식·같은 강조로 나열되니, 모델이 현재 장면 인물의 상태를 정밀하게 반영할 확률이 떨어진다. 자리에 없는 인물을 불러내는 사고(과거에도 있었던 '바론이 주변에 없을 때도 뜬다'류)의 온상이기도 하다.
- **근거**: `ids.forEach`에 slice가 없다. 인물 1명당 헤더 1줄(이름·관계·친밀도·상태·경지·note)과 관계 단계 1줄이 붙는데, 단계 문장은 리터럴이 길다 — 예컨대 [탐색]은 '이해관계로 얽힌 사이. 필요한 만큼 협조하되 등 뒤는 경계한다…'로 85자쯤이고 [신뢰]·[유대]는 더 길다. 인물당 대략 160~250자로 잡힌다(리터럴을 읽고 센 추정). ACADEMY_CHARACTERS는 24명이다(2822~2934 구간 실측). 전원을 만나면 이 블록만 4,000~6,000자가 매 턴 붙고, 여기에 4514~4530의 '단계가 오른 사람'/'아직 남인 사람' 목록이 같은 인물을 한 번 더 나열한다. 등록은 자동이고(대사 한 줄만 해도 11992에서 metChars에 박힌다) 제거·강등 경로는 어디에도 없다. 그리고 블록 머리에 '관계·상태 절대 뒤집지 말 것'이 전원에 걸린다.
- **수정**: metChars[id]에 lastSeenTurn을 기록하고(processResponse의 dialogue 순회에서), buildStatePrompt에서는 ①현재 장면 인물(_lastPresentIds) ②최근 10턴 내 등장 ③친밀도 40 이상 — 이 셋의 합집합만 전체 카드로 출력하고, 나머지는 '이름 친밀도' 한 줄짜리 목록으로 강등한다. 관계 단계 문장도 전원이 아니라 전체 카드 대상에게만 붙인다.

### 죽은 필드: metChars[].knows(소비자만 남고 생산자 없음)와 gameState.director(초기화·청소만 하고 아무도 안 읽음)

- **위치**: knows: buildStatePrompt 4651(읽기) / restoreAndReplay 10445(삭제) — 쓰기 코드 없음. director: 10378(init) / 10443(null 대입) — 읽기 코드 없음
- **증상**: knows는 '극적 아이러니 — 각 인물은 자기가 아는 것만 가지고 행동한다'는 기능의 전부였는데 완전히 정지 상태다. 그래서 A만 아는 비밀을 B가 아는 것처럼 말하는 사고를 막을 층이 지금 하나도 없다(LORE의 산문 규칙만 남았다). director는 세이브·스냅샷 60개에 null 필드로 실려 다니는 잔재라 해는 적지만, 코드를 읽는 사람에게 '아직 감독이 있나'로 오해된다.
- **근거**: 파일 전체 grep('knows')가 4651과 10445 두 줄뿐이다. 4651은 `if (m.knows) s += ... 지식: ... (모르는 정보는 절대 아는 것처럼 말하거나 행동하지 말 것)`으로 읽기만 하고, 10445는 이어하기마다 `delete _mc[_id].knows`로 지운다. state_update.chars 스키마(2371)에도 knows가 없고 병합부(11924~11940)에도 없다 — 채우는 코드가 어디에도 없다. 총감독 폐지(v297/2026-08-14) 때 생산자만 제거하고 소비자를 남긴 흔적이다. director도 같다: 10378에서 null로 만들고 10443에서 다시 null로 만들 뿐, 읽는 곳이 없다.
- **수정**: 둘 중 하나로 정리한다 — (a)knows를 되살릴 거면 state_update.chars 스키마에 `"knows": "이 인물이 아는 것/모르는 것(변화 시만)"`을 추가하고 병합부에서 이어붙이며 10445의 delete를 제거한다. (b)안 되살릴 거면 4651을 지운다. director는 init(10378)과 청소(10443)에서 함께 제거한다.

### 선행 사건에 매인 이벤트('학원제 직후', '개전 직후')는 eventDueTurn이 null이라 아예 후보에서 빠진다 — 1부→2부 전환이 코드의 손을 벗어난다

- **위치**: eventDueTurn() 4454~4462(마지막 `return null`) / dueEventNow() 4472~4473(`.filter(x => x.due !== null …)`) / ACADEMY_EVENTS 2941~2942
- **증상**: 1부(학원)에서 2부(국가전)로 넘어가는 축이 코드의 보장 없이 굴러가 개전이 영영 안 오거나, 반대로 아무 준비 없이 튀어나온다. 유저 관점에선 '학원제 끝나고 나서 이야기가 멈췄다'로 보인다.
- **근거**: eventDueTurn은 '개월/달', '주', '일차'만 파싱하고 그 밖은 null을 돌려준다. dueEventNow는 null을 필터로 잘라낸다. ACADEMY_EVENTS 9개 중 '개전 — 진영 선택'(cond: '학원제 직후')과 '완충령의 선택 — 가주의 항복 노선'(cond: '개전 직후 (진영 선택 이후)') 두 개가 정확히 이 구멍에 빠진다. 나머지는 1일차→1, 1개월→15, 2개월→30, 3개월→45, '4~5개월'→60(정규식이 4를 잡는다), 6개월→90으로 계산된다. 즉 코드가 밀어주는 사건이 45턴(학원제)에서 90턴(이졸데)으로 건너뛰고, 그 사이의 개전·진영 선택은 오직 모델의 자율에 맡겨진다. 하필 이 둘이 gameState.faction을 세우는 관문이고, faction은 buildStatePrompt 4610에서 '★주인공의 진영 = … (확정)'으로 매 턴 실리는 핵심 상태다.
- **수정**: eventDueTurn에 상대 조건을 추가한다: cond에 '직후'/'이후'가 있으면 null 대신 `(gameState._lastEventTurn || 0) + EVENT_MIN_GAP`을 반환하되, 선행 이벤트가 firedEvents에 있을 때만 후보가 되게 한다(events 배열의 앞 항목을 선행으로 간주하면 아카데미 배열 순서로 충분하다).

### 세이브 실패가 조용히 삼켜지는 경로 — IDB put 오류를 무시하고 성공(true)을 반환하며, 쿼터 복구 루프는 finding 1 때문에 gameHistory를 못 줄인다

- **위치**: _bulkPutIDB 8274 / bulkSetSilent 8313~8317 / boot 워치독 10283~10293 / autoSave 쿼터 복구 10233~10252
- **증상**: 평소엔 안 터지지만 터질 때 조용히 터진다. iOS PWA에서 워치독을 탄 세션은 localStorage 모드로 5MB를 두드리고, 실패해도 토스트 없이 지나가며, 새로고침하면 몇 턴 전으로 되돌아가 있다. 되감기 스냅샷은 그보다 먼저 조용히 사라진다.
- **근거**: 현재 정상 경로는 IndexedDB다(BULK_RE = /^vn_save_|^nakin_save$|^vn_slot_|^vn_snaps_/). 그래서 200~330KB 세이브가 localStorage 5MB를 직접 위협하진 않는다. 문제는 실패 감지다. `function _bulkPutIDB(k, v) { try { _bulkTx('readwrite').put(v, k); } catch {} }` — put은 비동기라 onerror/onabort를 안 달면 쿼터 초과·트랜잭션 중단이 catch에 절대 안 잡힌다. 그런데 bulkSetSilent는 `if (_bulkDB) { _bulk.set(k, v); _bulkPutIDB(k, v); return true; }`로 무조건 true를 돌려주고, autoSave는 그걸 성공으로 간주한다(메모리 캐시 _bulk엔 들어 있으니 그 세션 안에서는 정상으로 보이고, 새로고침해야 유실이 드러난다 — 2026-07-30 '되감기가 114턴만 뜬다' 사고와 같은 형태의 침묵 실패다). 둘째로 boot 워치독(10283~10293)이 4초 안에 IDB가 안 열리면 _bulkDB=null인 채로 localStorage 폴백 모드로 부팅하는데, 그 모드에서는 300KB 세이브 + vn_snaps_(SNAP_BYTES 상한 2MB)가 전 스토리와 5MB를 나눠 쓴다. 주석은 '매턴 클라우드 백업이 있어 유실 없음'이라 적었지만 scheduleAutoBackup은 동기화 암호가 설정되기 전엔 no-op이다(10248 주석). 셋째로 autoSave의 쿼터 복구 루프(10238~10245)는 `if (!trimHistory(cut) && dialogueLog.length <= 60) break;`인데, finding 1 상태에서는 trimHistory가 항상 0이라 gameHistory는 한 줄도 못 줄이고 dialogueLog만 깎다가 끝난다.
- **수정**: _bulkPutIDB를 Promise로 만들어 onerror/onabort/transaction.onabort를 잡고, bulkSetSilent가 그 결과를 반영하도록 한다(최소한 실패 시 window 이벤트나 토스트). 쿼터 복구 경로에는 summaryIndex를 무시하는 강제 절단(앞에서 N개를 버리고 summaryIndex를 0으로 클램프 — 요약이 아직 안 돈 구간이라도 원문 유실보다 저장 실패가 더 나쁘다)을 추가한다.


---

## LOW

### 일차 조건과 개월 조건의 환산 스케일이 서로 어긋난다 (1개월=15턴인데 30일차=30턴)

- **위치**: novel.html:4453-4461 TURNS_PER_MONTH / eventDueTurn
- **증상**: 아카데미는 1일차 조건 둘뿐이라 당장은 티가 안 나지만, 낙인의 검류의 '7일차/15일차/30일차' 타임라인이나 편집기로 만든 커스텀 스토리 events가 일차로 적히면 사건 간격이 의도의 2배로 늘어져 같은 '사건이 안 온다' 증상이 재발한다.
- **근거**: eventDueTurn은 '개월'을 15턴으로 압축하면서 '일차'는 `return parseInt(m[1],10)`으로 1일=1턴 그대로 둔다(4459). 같은 함수 안에서 서사 한 달이 15턴이면 서사 하루는 0.5턴이어야 일관되는데, 일차 조건만 실시간 환산 없이 통과한다.
- **수정**: 일차 조건도 동일 스케일로 환산한다(`Math.max(1, Math.round(TURNS_PER_MONTH/30*day))`) 또는 스토리별로 '일차=턴' 여부를 명시적 플래그로 선언하게 한다.

### 실시간 경과 시간 건너뛰기(_timeSkipNote)는 장면 상태를 보지 않는다

- **위치**: novel.html:10458-10460 restoreAndReplay / 4686-4690 주입문 / 14696 timeflowOn
- **증상**: 이 옵션을 켠 유저가 대화 한복판에서 앱을 닫고 다음 날 돌아오면, 복귀 첫 턴에 대사가 이어지지 않고 시간이 점프한다 — 백스톱과는 다른 경로로 같은 '대화 중 아침' 체감이 발생한다.
- **근거**: 세이브 후 3시간 이상 지나 이어하기를 하면 `window._timeSkipNote`가 세팅되고(10458-10460), 다음 프롬프트에 '현실로 약 N시간이 지났다 … 시간대 갱신'이 실린다(4689). 지금 장면이 대화 도중인지, 어디서 끊겼는지는 검사하지 않는다. 다만 timeflowOn은 localStorage 옵트인이라 기본값은 꺼짐이다(14696).
- **수정**: 직전 턴이 미결 장면(선택지 대기·대사 진행 중)이면 스킵 노트를 '장면을 매듭지은 뒤 경과를 반영하라'로 완화하거나, 한 턴 미뤄서 주입한다.

### background 지시문의 예시 키가 무협 스토리 전용이라 아카데미에서는 목록에 없는 키를 예시로 준다

- **위치**: callGemini 11399행(bgKeys 지시문) / STORY_BG_FILES 13086행
- **증상**: 모델이 예시를 따라 목록 밖 키를 쓰면 배경이 안 바뀐다(지시문 스스로 그렇게 경고한다). 아카데미에서 장면이 바뀌었는데 배경이 그대로인 증상의 후보 원인이다.
- **근거**: 11399행은 모든 스토리에 같은 문장을 보낸다: "background 필드 — … 아래 이 스토리의 배경 키 목록에서만 정확히 골라라(목록에 없는 이름을 쓰면 배경이 안 바뀐다). **저잣거리=market_street, 객잔/주막=inn_hall, 나루터=river_ferry, 마을=herb_village 등**". 13086행 STORY_BG_FILES의 academy 항목은 academy_courtyard·classroom·duel_arena·forbidden_library 등 19개이고 market_street·inn_hall·river_ferry·herb_village는 **하나도 없다**(넷 다 murim 전용 키다). 즉 "목록에서만 골라라"고 한 뒤 목록 밖 키 4개를 예시로 제시한다.
- **수정**: 예시를 하드코딩하지 말고 해당 스토리의 bgKeys 앞 3~4개를 동적으로 뽑아 쓰거나, 예시 절을 통째로 삭제한다(목록이 이미 키 이름을 그대로 보여준다).

### "변화 없는 필드는 넣지 말 것"이 여러 '반드시 채워라' 지시 및 프라이밍 예시와 어긋난다

- **위치**: ENGINE_RULES 2393행 vs 2325행(choices)·2380행(cg)·2378행(time) / PRIMING_SHOTS 10603~10606행
- **증상**: 필드 포함/생략 기준이 턴마다 흔들린다. 특히 time 필드가 '변화 없으면 생략' 쪽으로 기울면 v318이 고친 시간 정체가 재발한다(v315 이전 증상: 54턴에 2일차).
- **근거**: 2393행: "변화가 없는 필드는 넣지 말 것 (토큰 절약)." 그런데 2380행은 "장면이 끝나면 **반드시 null**"이라며 cg를 명시적으로 담으라 하고, 2325행은 choices를 매 응답 채우라 하며, 2378행은 time을 적극적으로 갱신하라 한다. 동시에 10603~10606행의 프라이밍 예시 응답은 `background: null, state_update: { rep: 0, gold: 0, location: null, status: null, flags: {} }`로 **변화 없는 필드를 전부 담아** 2393행이 금지한 형태를 시연한다.
- **수정**: 2393행을 "단 choices·time·cg는 이 규칙의 예외로, 위 각 필드의 지시를 따른다"로 한정하고, PRIMING_SHOTS의 state_update 예시에서 0/null 필드를 제거한다.

### 스트리밍 중 적용한 배경이 lastBg에 반영되지 않아, 스트림 중단 턴에서 화면·기록(bgFile)과 세이브(lastBg)가 어긋난다

- **위치**: extractStreamUnits 11234-11236, onChunk 11466·11477(setBg만 호출), processResponse 11866-11869(lastBg 대입), 스트림 에러 조기 반환 11534-11541
- **증상**: 스트림이 끊긴 턴 뒤 이어하기를 하면 `setBg(lastBg)`가 옛 배경을 깔아, 방금 읽은 장면의 기록 배경과 화면 배경이 다르다. 성애 장면에서는 스트림 중 떴던 CG가 응답 파싱 실패 시 끝에서 사라진다.
- **근거**: 질문의 '스트림 중 값 정정' 자체는 구조상 발생하지 않는다 — 한 JSON 객체에 background/cg/mood는 각 1회뿐이고, 정규식 `"background"\s*:\s*"([^"]*)"`는 닫는 따옴표가 도착해야 매칭되며(부분 값은 매칭 안 됨), 최종 processResponse가 `data.background`를 무조건 다시 setBg 하므로 값은 완료 시점에 교정된다. 문제는 **lastBg**다: 스트림 경로는 setBg만 부르고 lastBg를 안 쓴다. lastBg는 processResponse에서만 대입되는데, `if (stream.active && (result.error || result.blocked || !text))` 분기(11534)는 **processResponse를 건너뛰고 return**한다. 그런데 v306이 showNextDialogue의 큐 소진 지점에 autoSave를 새로 넣어(12341-12343) 이 턴도 저장된다 → 세이브의 lastBg=옛 배경, dialogueLog 항목의 bgFile=새 배경. 부수적으로 스트림이 CG를 띄운 뒤 최종 파싱이 extractFallback으로 떨어지면 `data.cg` 부재 → hideEventCG로 CG가 사라진다(11871).
- **수정**: onChunk에서 setBg 할 때 `lastBg = u.background;`를 같이 대입한다(cg도 필요하면 동일). 그리고 스트림 에러 조기 반환 전에 현재 표시 상태를 lastBg에 확정한다.

### v305에서 들어온 정규식 lookbehind가 파일 유일 사용처 — iOS Safari 16.4 미만에서 스크립트 전체가 파싱 실패

- **위치**: _isNarrationEcho 10167 `.split(/(?<=[.!?…。])\s+|
+/)`
- **증상**: 구형 iOS 기기/구버전 사파리에서 게임이 흰 화면으로 안 열린다. 오늘 이전 버전은 열렸으므로 회귀로 인식된다(유저가 그런 기기를 안 쓰면 무증상 — 확인 필요).
- **근거**: `grep -c "(?<=" novel.html` = 1. 즉 오늘 추가된 이 한 줄이 파일 전체에서 유일한 lookbehind다. 정규식 리터럴은 스크립트 파싱 시점에 컴파일되므로, lookbehind 미지원 엔진에서는 함수 호출 여부와 무관하게 **스크립트 전체가 SyntaxError**로 죽는다(단일 HTML 파일이라 앱이 아예 안 뜬다). JS 정규식 lookbehind는 Safari 16.4(2023-03) 이상 지원 — 그 이전 iOS는 미지원.
- **수정**: lookbehind 없이 같은 분할을 한다: `String(narration).split(/\n+/).flatMap(p => p.match(/[^.!?…。]+[.!?…。]*/g) || [])` 처럼 match 기반으로 교체.

### gameState에 쌓이지만 아무도 안 읽는 죽은 필드: director, day

- **위치**: director 초기화 10388 / 로드시 null 대입 10453, day 초기화 2206·10383
- **증상**: 직접적인 플레이 증상은 없지만, day가 존재하는 탓에 '일차가 상태로 관리된다'는 착각을 준다. 실제로는 모델이 쓴 문자열 파싱이 유일한 진실원이라, 모델이 '늦은 밤' 같은 미등록 표기를 쓰면 advanceTimeSlotIfStale이 손을 떼고(11848~11852) 날짜 진행이 조용히 멈춘다 — 숫자 필드가 있었다면 방어할 수 있는 지점이다.
- **근거**: grep 결과 `director`는 신규 게임 초기화(10388)와 로드 시 청소(10453)에만 등장하고 읽는 코드가 없다(총감독 시스템은 2026-08-14 폐지, 주석 10697~10700). `gameState.day`는 2206·10383에서 1로 초기화될 뿐 갱신도 참조도 되지 않는다 — 실제 날짜는 gameState.time 문자열의 'N일차'를 정규식으로 파싱해 쓴다(4514, 4623, 11840 등).
- **수정**: director는 초기화·청소 코드까지 제거한다. day는 제거하거나, 반대로 진짜 진실원으로 승격시켜(time 파싱 결과를 매 턴 day에 반영, 역행 금지 클램프) 시간 진행 백스톱이 문자열이 아니라 숫자를 보게 한다.

### 첫 요약이 프롤로그 8엔트리짜리인데 '[지금까지의 이야기 요약]'이라는 라벨로 프롬프트 맨 앞에 실린다

- **위치**: summarizeIfNeeded 10758 (`toSummarize = gameHistory.slice(summaryIndex, len - _win)`) / buildMessages 10614~10622
- **증상**: 수십 턴 진행된 시점에 '지금까지의 이야기 = 입학식 전 프롤로그'가 최상단에 놓이는 셈이라, 모델이 현재 진행 단계를 오판할 여지를 준다(첫날 프레이밍 반복은 이미 4507~4512에서 별도 교정약을 붙여야 했던 고질이다 — 같은 병의 다른 입구다).
- **근거**: finding 1의 교착이 풀리는 순간(원문 총량이 12만 자를 막 넘긴 턴)에 _win은 여전히 원문 대부분을 덮으므로 `len - _win`은 8~10 남짓이다. 즉 첫 요약의 대상은 gameHistory의 맨 앞 8개 — 프롤로그·모드 선택·캐릭터 설정 구간이다. 그렇게 만들어진 텍스트가 buildMessages에서 `[지금까지의 이야기 요약]\n${storySummary}\n\n[이야기 계속]`으로 PRIMING_SHOTS 바로 뒤, 원문 133엔트리 앞에 놓인다. 라벨과 내용의 범위가 어긋난다.
- **수정**: 라벨에 구간을 명시한다: `[1~${summaryIndex}번째 장면까지의 요약 — 그 이후는 아래 원문 그대로]`. finding 1의 SUMMARY_KEEP 고정을 적용하면 요약 구간이 정상적으로 앞에서부터 자라므로 이 어긋남 자체도 함께 해소된다.

