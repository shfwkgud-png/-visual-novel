# PANDORA 아카데미 + 공용 엔진 설계 감사 보고서 (내 워크플로우)

에이전트 17개 / 145만 토큰 / 도구 호출 300회 / 결함 후보 40건 -> 적대적 검증 12건 확증

---

# 근본 원인 진단

## 1. 요청 조립과 상태 커밋이 분리돼 있지 않다

`buildStatePrompt()`는 이름과 달리 순수 함수가 아니다. 4677-4680에서 `firedEvents.push`와 `_lastEventTurn` 대입을 한다 — **프롬프트 문장을 만든 행위가 곧 "그 사건은 일어났다"는 영구 확정**이다. 반면 정상 커밋(gameHistory.push + turnCount++)은 11655-11656에 있고 응답 파싱 성공 후에만 실행된다. 상태 갱신 지점이 두 시점에 흩어져 있고, 앞쪽 지점은 실패를 모른다.

이 파일에서 buildStatePrompt는 한 턴에 여러 번 호출된다. callGemini 재시도 4경로(429 11552, 5xx·타임아웃 11561, blocked 11575, 빈 응답 11590)가 전부 자기 자신을 재귀 호출해 11401에서 프롬프트를 다시 만들고, regenerate()도 15203에서 다시 부른다. 그때 `dueEventNow()`는 4470의 `tc - _lastEventTurn = 0 < EVENT_MIN_GAP(8)`에 걸려 null을 반환한다. 즉 **사건은 firedEvents에 박혀 있는데 프롬프트에서는 사라진다.** 429 한 번 맞고 자동 재시도로 성공한 턴조차 사건만 조용히 증발하고, 4473의 `!fired.includes()` 필터가 그 사건을 영구히 후보에서 뺀다.

안내와 실제도 어긋난다. 에러 메시지(11543/11566)는 "[↺ 다시 답변]을 눌러 재시도하세요"라고 하지만 `regenerate()`(15183-15204)는 gameHistory.pop과 turnCount-- 만 하고 gameState는 손대지 않는다. 안내대로 눌러도 사건은 돌아오지 않는다. 복구되는 유일한 경로는 15366 `restoreSnap`의 gameState 통째 교체(수동 되감기 UI)뿐이다.

감사 결함 4건이 전부 이 한 곳에서 나온다.

## 2. 앱↔모델 계약이 단방향이다 — 코드가 프롬프트의 약속을 이행하지 않고, 모델이 준 값은 검증 없이 받는다

- **받아야 할 것을 안 받는다.** 2384 스키마가 매 턴 `chronicle` 배열을 요구하고 2395가 "이것이 장기 기억이다. 기록 안 하면 몇 턴 뒤 그 사실은 소멸한다"고 약속한다. 그런데 `gameState.chronicle`에 쓰는 코드는 파일 전체에서 10829-10831(요약 패스 내부) 한 곳뿐이고, `processResponse`(11830~)에 `data.chronicle`을 읽는 줄이 없다. 모델이 1차로 확정 선언한 필드를 버리고, 같은 사실을 2차 요약 모델이 산문에서 되추출하게 시키는 구조다.
- **검증 없이 받는다.** 11864 `if (s.time && !_isIdol) { gameState.time = s.time; resetTimeSlot(); }` — 값이 바뀌었는지 보지 않는다. 바로 위 11857의 location은 비교를 한다. 모델이 같은 시간대 문자열을 재기입만 해도 `_slotTurns`가 1로 고정되고 `_slotMoved`가 지워져, v318이 남긴 안전망 두 개(넛지 3턴, 백스톱 10턴)가 동시에 죽는다.
- **확인 신호가 없다.** 4670 사건 주입문은 "실제로 일으켜라"라고 요구하지만 모델이 그 장면을 냈는지 앱이 확인할 수단이 없고, 확인하지 않은 채 커밋한다.

## 3. 진행 상태가 서로를 모르는 세 축으로 쪼개져 있고, 그중 하나는 자유 문자열 파싱에 의존한다

- `turnCount` — 사건 도래(4452-4478). 응답 성공마다 +1.
- `gameState.time` — 문자열. 상태창(15047)·프롬프트(4611)·경과일 주입(4508)·넛지·백스톱이 여기 매인다.
- `_slotTurns` / `_slotMoved` — 백스톱 전용.

세 축을 잇는 코드가 없다. 아카데미는 `initialState.time`이 `'입학식 아침'`(8003)이라 `\d+\s*일차` 정규식에 안 걸려 `timeFlowNudge`(11805)와 `advanceTimeSlotIfStale`(11816)이 **1턴째부터** 둘 다 즉시 return한다. 그동안 사건은 턴 기준으로 정상 발동하므로, 상태창은 '입학식 아침'인데 15턴째에 '입학 1개월 후' 서열전이 열린다.

같은 성질의 조용한 무동작이 이벤트 조건에도 있다. `eventDueTurn`(4454-4461)은 '학원제 직후' 같은 체인 조건에 null을 반환하고, 주석은 "모델 판단에 맡긴다"고 적혀 있다. 숫자 조건 5건(서열전 15 / 소환장 30 / 학원제 45 / 세드릭 60 / 이졸데 90턴)은 "이번 턴에 실제로 일으켜라"는 강제 블록을 받는데, **1부→2부 개전 전환만 ACADEMY_LORE 2921-2922의 산문 한 줄에 맡겨져 있다.** 중요도가 클수록 강제력이 약한 역전이다.

파싱이 실패할 때 앱이 손을 떼지 않고 추측하는 것도 같은 뿌리다. 11823 `if (i < 0) i = TIME_SLOTS.indexOf('오전')` — 시간대 단어를 못 읽으면 현재 위치와 무관하게 오후로 리셋한다('2일차, 밤', '2일차 늦은 밤', '심야' 등에서 시간이 뒤로 간다).

## 4. 규칙을 증상마다 텍스트로 덧대 왔고, 조항 간 충돌을 아무도 검출하지 않는다

- 2325: "choices를 절대 생략하지 마라. 빈 배열·null도 금지. 최소 2개" (근거로 2026-07-28 유저 지적이 주석에 명시)
- 2383 / 2447: "전투·충돌·판정이 걸린 장면에서는 반드시 빈 배열 []" (근거로 2026-08-16 유저 지시가 명시)

같은 요청 안에 정면으로 반대되는 두 명령이 들어간다. 두 조항 모두 각각의 유저 지적을 막으려 추가됐으므로, 지금 구조는 **두 과거 지적이 서로를 되살린다.** 게다가 ENGINE_MOD_RPG는 프롬프트 끝에 붙어(11357) 전투 측 지시가 위치상 뒤에 놓인다.

판정 주체 문제이기도 하다. "지금이 전투인가"를 앱이 모르니 모델에게 맡기는데, 앱은 빈 배열을 그대로 존중하므로 모델이 잘못 판단한 턴은 화면에 입력창만 남는다. 판단을 모델에게 맡길 거면 그 판단을 구조화 필드로 받아 앱이 처리해야 하는데, 지금은 판단도 실행도 전부 산문 지시 안에서 끝난다.

---

# 우선순위별 조치

순위 기준: ①세이브를 영구 손상시키는가 ②플레이어가 장면을 통째로 못 보는가 ③안전장치가 죽어 있는가 ④턴 단위 품질인가.

## P0-1. 이벤트 커밋을 프롬프트 조립에서 턴 성공 지점으로 이동 — 규모: 보통

**왜 첫째**: 유일하게 세이브에 영구 흔적을 남기는 결함이고, 감사 결함 4건이 전부 여기서 파생한다. 지금 진행 중인 세이브는 429 한 번, 재생성 한 번마다 메인 사건을 하나씩 잃고 있으며 되돌릴 UI 경로가 안내와 다르다.

- 4677-4680의 push/대입 삭제 → `window._pendingEvent = { t: _due.t, turn: turnCount }` 만 세운다.
- `dueEventNow()`가 `_pendingEvent`를 최우선 반환하도록 한다. **이 처리가 없으면 커밋 시점만 옮겨도 재시도 턴은 여전히 사건 없이 나간다**(4470 게이트 때문).
- 11655 `turnCount++` 직후에만 `firedEvents.push` + `_lastEventTurn = turnCount`.
- catch(11663)와 callGemini early return 4경로(11539-11547, 11564-11569, 11571-11581, 11587-11592)에서 `_pendingEvent`를 유지(다음 턴 재주입) 또는 폐기 — 사건 보존 관점에선 유지가 낫다.
- `regenerate`(15183)·`undoInput`에서 `_pendingEvent` 폐기 + `if (gameState._lastEventTurn > turnCount) gameState._lastEventTurn = null` 보정.
- 검증: fetch를 강제 실패시켜 1턴 실패 → 재시도 성공 시 프롬프트에 사건 블록이 실리는지, firedEvents가 1만 늘어나는지 확인.

## P0-2. 기록 모드 보류분이 화면에서 통째로 증발 — 규모: 작음 (한 줄)

**왜 둘째**: 수정이 한 줄인데 증상이 "턴 하나가 화면에 한 줄도 안 나오고 선택지만 뜬다"이다. 대사는 10117-10123에서 이미 dialogueLog에 들어가므로 백로그에는 있는데 화면에서는 본 적 없는 장면이 생긴다.

경로 확인 완료: 12038 `resetTurnLineKeys()` → 12060 `enqueueLine`으로 키 등록 → 12069 historyMode 분기가 큐를 `_pendingDialogues`로 옮기고 return → 12718 `exitHistory`가 `showDialogue(pd)` → 15154 `forEach(enqueueLine)` → 10113에서 키가 이미 존재해 전량 return. 이 사이에 리셋이 없다.

- 10110을 `if (d && !d.isPlayerLine && !d._log)`로 바꾼다. `_log`가 붙은 줄은 이번 턴에 이미 정상 접수된 보류분이고(10121-10123에서 부여), v309가 막으려던 완료 보정 재투입분은 12019·12021에서 항상 새 객체로 만들어져 `_log`가 없으므로 계속 차단된다.
- 같이 처리: 스트리밍 중 ◀를 누르면 12008 `streamed` 분기에 historyMode 가드가 없어 12027 `showNextDialogue()`가 기록 화면을 덮어쓴다. 12027 앞에 동일 가드가 필요하다.

## P0-3. 사건 반영 확인 신호 — 규모: 작음

**왜**: P0-1은 "실패 시 소실"을 막지만 "모델이 무시했을 때"는 남는다. 7만 자 프롬프트에서 한 문단이 묻히는 건 흔하다.

- 4670 주입문에 "이 사건을 실제로 일으켰으면 `state_update.event`에 사건명을 그대로 적어라" 한 줄 추가.
- 커밋 조건을 `s.event === _pendingEvent.t`(또는 chronicle/flags에 사건명 포함)로. 미확인이면 `_pendingEvent`를 유지해 다음 턴 재주입, 3턴 후 포기하고 커밋.
- `_pendingEvent`가 있는 동안은 4470의 EVENT_MIN_GAP 게이트를 건너뛴다.

## P1-4. time 리셋을 값이 실제로 바뀐 턴에만 — 규모: 작음 (한 줄)

**왜**: 한 줄로 v318 안전망 두 개를 되살린다. 단 아카데미에서는 P1-5 없이는 효과가 없다(정규식에 안 걸림).

```
if (s.time && !_isIdol) {
  if (String(s.time).trim() !== String(gameState.time||'').trim()) { gameState.time = s.time; resetTimeSlot(); }
}
```

추가로 `_slotMoved`를 `resetTimeSlot`에서 분리할 것. 지금은 11857이 장소 이동을 기록한 직후 11864가 그것을 지운다 — 백스톱(11819)이 요구하는 **유일한 증거**인데, 장소를 옮긴 그 턴에 모델이 같은 time을 한 번 되쓰면 사라진다.

2366 스키마 문구도 "값이 바뀔 때만 넣어라. 같으면 넣지 마라"로 바꿔 되쓰기 유인을 없앤다(2393의 "변화 없는 필드는 넣지 말 것"과 이미 문면이 충돌한다).

*미검증*: 모델이 실제로 같은 값을 되쓰는 빈도는 실측하지 않았다. 이 결함은 되쓰는 턴에만 발현하는 조건부 구멍이다. 리셋 조건이 값 변화가 아닌 것과 `_slotMoved`가 함께 지워지는 것은 코드 사실이다.

## P1-5. time 표기 정규화 + 파싱 실패 시 무동작 — 규모: 보통

**왜**: 아카데미는 time 기반 장치 3개(넛지·백스톱·경과일 주입 4508)가 첫 턴부터 전부 죽어 있다.

- 8003 `initialState.time`을 `'1일차 아침 (입학식)'`로. (2859·8015·8016·8017의 '입학식 아침' 서술 텍스트는 그대로 둬도 무방)
- 스토리 시작·세이브 로드 한 곳에서 정규화: time에 '일차' 표기가 없으면 `'1일차 '`를 앞에 붙인다.
- 11816의 `(\S+)?`를 `(아침|오전|오후|저녁|밤)` 화이트리스트로 바꾸고 별칭 매핑(새벽→아침, 정오→오전, 심야·한밤중→밤, 방과후→저녁)을 붙인다. 그래도 못 읽으면 11823처럼 오후로 추측하지 말고 `return null`.

## P1-6. choices 규칙 단일화 — 규모: 작음

**왜 이 위치**: 상태를 손상시키지 않지만 매 턴 발생하고 수정 비용이 거의 없다.

- 2325에 예외를 흡수한다: "평시에는 2~4개 필수(빈 배열·null 금지). 단 전투·충돌·추격 등 판정이 걸린 턴에만 빈 배열." 2383·2447의 중복 조항은 이 문장을 참조만 하도록 축약.
- 더 확실한 형태: `state_update`에 `combat: true/false`를 받고 앱이 전투 턴에만 choices를 무시한다. 규칙이 "combat을 정확히 표시하라" 하나로 줄고, 모델이 틀려도 앱이 관측·로깅할 수 있게 된다.

## P2-7. chronicle 수신 배선 — 규모: 작음

**왜 P2**: 응답 원문이 최근 창 안에 있는 동안은 10625-10626이 chronicle 포함 JSON을 그대로 재전송하므로 즉시 체감되지 않는다(gemini 12만 자, NIM 128k ctx는 `modelCtxTokens()*0.40 = 51,200`자). 소실은 창 밖으로 밀릴 때 일어난다.

- `processResponse`의 state_update 처리 직후에 `data.chronicle` 수용 + 중복 제거 + 기존 상한 60 트림.
- 현재 유일한 기록 경로 10819-10831은 (a) try/catch로 실패가 조용히 삼켜지고 (b) `if (summary && summary.trim())` 안에 있어 요약 실패 시 같이 죽으며 (c) 프롬프트가 "확정 사건"만 요구해 '유저 설정: …' 류 비사건 설정을 형식상 걸러낼 수 있다. 1차 소스를 직접 받는 편이 맞다.

## P2-8. 요약 트리거의 자기참조 제거 — 규모: 보통

**왜**: 10755 `if (unsummarized < SUMMARY_INTERVAL + _win) return;`의 `_win`이 최근 창 크기(2188 `recentWindowCount`)라, 히스토리 누적 글자수가 예산에 닿기 전까지 요약이 수학적으로 한 번도 발화하지 않는다. gemini 경로 대략 40~55턴, NIM 기본 대략 20턴. 그 구간에서 storySummary와 chronicle이 둘 다 비어 있고, trimHistory도 무력해 gameHistory가 예산까지 계속 자란다(세이브 200~330KB의 몸통).

- 조건을 창 크기와 분리: 창 밖으로 밀린 항목이 생겼거나, 마지막 요약 이후 8턴이 지났으면 실행.
- 연대기 추출을 요약 성공 여부에서 분리한다(P2-7이 들어가면 백업 역할로만).
- 발화 이후에도 `HISTORY_MAX(60)`은 실질 구속력이 없다(결과 길이가 `max(60, _win)`). 세이브 크기를 잡으려면 항목 수가 아니라 글자 예산 쪽을 봐야 한다.

## P2-9. 2부(개전) 전환의 결정론화 — 규모: 보통

**왜 뒤**: 45턴 이후에야 문제가 되고, 앞의 수정들이 들어가야 그 지점까지 도달한다.

- `eventDueTurn`에 체인 처리 추가: cond에 'X 직후'가 있으면 X의 실제 발동 턴 + 2~3을 반환. 그러려면 firedEvents를 제목 배열이 아니라 `{t, turn}`으로 바꾸거나 별도 맵을 둔다 — P0-1에서 커밋 지점을 어차피 손대므로 함께 처리하는 게 싸다.
- 세드릭 사건(cond `'입학 4~5개월 (개전 전후)'`)은 4672가 cond 문자열을 그대로 프롬프트에 싣기 때문에, 개전이 안 난 상태로 60턴에 '개전 전후'라는 전제가 주입된다. 체인이 들어가기 전까지는 cond 주입을 숫자 부분만으로 자르는 임시 처리가 필요하다.

---

# 하지 말아야 할 것

**1. 증상 하나에 조항 하나를 더 붙이는 방식.** choices가 그 결과물이다 — 2026-07-28 지적으로 2325가, 2026-08-16 지적으로 2383·2447이 추가됐고 지금은 서로를 무효화한다. 규칙을 넣기 전에 같은 필드를 언급하는 기존 조항을 전부 grep하고, **추가가 아니라 교체**할 것. 한 필드에 대한 명령은 프롬프트 안에 한 곳에만 있어야 한다.

**2. 프롬프트 문장으로 코드 결함을 덮기.** 2395는 "chronicle에 적으면 영구 보존된다"고 약속하는데 읽는 코드가 없다. 4670은 "실제로 일으켜라"라고 하는데 일으켰는지 확인하지 않는다. 이행되지 않는 약속은 토큰만 쓰는 게 아니라 디버깅을 방해한다 — 프롬프트에 지시가 있으니 원인이 프롬프트 밖에 있다는 걸 놓치게 만든다.

**3. 앱이 맥락을 모르면서 상태를 덮어쓰기.** v318이 "앱이 장면을 모르면 시간에 손대지 않는다"로 재설계했는데, 11823은 단어를 못 읽으면 오후로 추측해 덮어쓴다. 못 읽으면 return이 원칙이다. 뒤집힌 형태인 **조용한 무동작을 관측 불가능하게 두는 것**도 같은 병이다 — `eventDueTurn`의 null, `timeFlowNudge`의 `''`, 10819의 빈 catch는 전부 "아무 일도 안 일어나는데 아무도 모르는" 상태를 만든다. 최소한 콘솔 경고 한 줄은 남길 것.

**4. 순수해 보이는 이름의 함수에 상태 변경 넣기.** buildStatePrompt는 "프롬프트를 만든다"는 이름으로 사건을 확정한다. 이 파일에는 같은 함수를 한 턴에 여러 번 부르는 경로가 최소 6개(재시도 4 + regenerate + 잘림 복구 11636) 있으므로, 조립 함수의 부작용은 **반드시** 중복 실행된다. 조립 계열 함수에는 gameState 쓰기를 넣지 말 것.

**5. 복구 UI가 상태 모델을 모른 채 텍스트만 되돌리는 것.** regenerate는 gameHistory·turnCount만 되돌려서, 에러 안내가 권하는 "↺ 다시 답변"이 실제로는 사건 소실을 확정한다. 상태를 되돌리는 로직은 `restoreSnap`(15366)에 이미 있다 — 새로 쓰지 말고 재사용하거나, 최소한 되돌릴 키 목록을 한 곳에 모아 두 함수가 같은 목록을 참조하게 할 것.

**6. 서사의 뼈대를 모델 준수에 인질로 맡기기.** 숫자 조건 사건 5건은 "이번 턴에 일으켜라"는 강제 주입을 받는데, 1부→2부 전환은 산문 한 줄뿐이다. 중요도와 강제력이 반비례하고 있다. 페이즈 전환처럼 되돌릴 수 없는 진행은 앱이 소유해야 한다.

**7. 한 번에 두 변수 이상 바꾸고 실측 없이 넘어가기.** v315(3턴 강제) → v318(재설계)은 진행 규칙을 다시 썼지만 그 값을 실제로 갱신하는 지점(11864)은 건드리지 않았고, 그래서 재설계한 장치가 조건부로 죽어 있다. 이번 수정도 P0-1 → 실패 주입 테스트 → P0-2 → 기록 모드 왕복 테스트 순으로, 하나씩 넣고 재현 경로로 확인할 것.