# PANDORA 통합 감사 44건 처리 결과 보고서

원본: `PANDORA_통합_감사_보고서_1.md`(보고서A 54건 + 보고서B 12건 병합, 원문 불릿 직접 카운트 44건)
처리: v320 ~ v327 (커밋 4개)
결과: **42/44 완료**, 2건 의도적 보류(사유 명시)

---

## 3장 — 우선순위 조치 (P0~P2, 9건)

### P0-1. 이벤트 커밋을 프롬프트 조립 시점에 하던 것

**원인**: `buildStatePrompt()`가 "지금 일어날 사건" 문장을 프롬프트에 붙이는 바로 그 자리에서 `gameState.firedEvents.push()`를 실행했다. 즉 프롬프트를 **만든 행위 자체**가 "그 사건은 일어났다"는 영구 확정이었다. 문제는 이 함수가 한 턴에 여러 번 불릴 수 있다는 것 — API 재시도 4경로(429·5xx·차단·빈응답)와 [다시 답변]이 전부 이 함수를 다시 부른다. 재조립 시점엔 `EVENT_MIN_GAP` 간격 게이트에 걸려 `dueEventNow()`가 null을 반환하므로, **사건은 firedEvents에 이미 박혀 있는데 정작 그 사건을 담은 프롬프트는 모델에게 한 번도 전달되지 않는** 상황이 생겼다. 429 한 번, [다시 답변] 한 번에 메인 사건이 통째로 소실되고 복구 경로가 없었다.

**해결방법**: 조립 시점엔 `window._pendingEvent = { t: 사건명, tries: 시도횟수 }`만 세우고, 실제 확정은 응답이 성공적으로 파싱된 뒤(`turnCount++` 직후) 한 곳에서만 하도록 커밋 지점을 이동. `dueEventNow()`는 `_pendingEvent`가 있으면 무조건 그 사건을 최우선으로 반환해 재조립 시에도 같은 사건이 실리게 했다. `regenerate()`·`undoInput()`(되돌리기)에는 그 턴에 확정된 사건을 되돌려 다음 시도에 다시 실리게 하는 복구 로직을 추가했다.

**해결결과**: 노드 시뮬레이션으로 3가지 시나리오 검증 — ①API 429 → 재시도 성공 시 같은 사건이 다시 실리고 1회만 확정 ②[다시 답변] → 되돌린 뒤 재조립에 사건이 다시 등장, 최종 1회만 확정 ③직후 턴은 간격 게이트로 정상적으로 null. 라이브 v320 배포, 2회 검증.

---

### P0-2. 기록 모드 보류분이 화면에서 통째로 증발

**원인**: 기록(◀)을 보는 도중 응답이 도착하면 `_pendingDialogues`로 보류되고, 기록 모드를 빠져나올 때(`exitHistory`) `showDialogue()`로 다시 재생된다. 그런데 v309에서 추가한 `_turnLineKeys`(같은 턴 재투입 차단)가 이 보류분의 키를 **이미 등록해 둔 상태**라서, 되돌아온 콘텐츠를 "재투입"으로 오인해 전량 차단했다. 결과: 그 턴 대사가 화면에 한 줄도 안 나오고 선택지만 뜨는데, 백로그(`dialogueLog`)에는 정상적으로 들어가 있어 원인 추적이 어려웠다.

또한 재검증 과정에서 추가로 발견: 스트리밍 완료 처리 분기(`if (streamed) {...}`)에 `historyMode` 가드가 아예 없어서, 스트리밍 도중 사용자가 ◀를 눌러도 응답 완료 시점에 `showNextDialogue()`가 무조건 호출돼 기록 화면을 덮어썼다.

**해결방법**: `enqueueLine`에서 `_log`가 이미 붙은 항목(=이번 턴에 정상 접수된 보류분)은 재투입 차단 검사를 건너뛰도록 조건 수정. 완료 보정 재투입분은 항상 새 객체라 `_log`가 없으므로 계속 정상 차단된다. 스트리밍 완료 분기에도 비스트리밍 분기와 동일한 `historyMode` 가드(보류 처리)를 추가.

**해결결과**: 두 수정 모두 v320·v322에 반영, 문법 검사 통과. 라이브 배포·검증 완료.

---

### P0-3. 사건 반영 확인 신호 (신규 발견)

**원인**: P0-1로 "사건 소실"은 막았지만, "모델이 사건 블록을 프롬프트에서 받고도 무시했을 때"는 여전히 남아 있었다. 7만 자짜리 프롬프트에서 한 문단이 묻히는 건 흔한 일인데, 기존 로직은 **응답이 성공하기만 하면 그 내용과 무관하게 무조건 확정**했다.

**해결방법**: 사건 주입문에 "이 사건을 실제로 열었으면 `state_update.event`에 사건명을 그대로 적어라"를 추가. 커밋 조건을 `state_update.event`가 사건명과 일치하거나 `chronicle`에 사건명이 포함된 경우로 좁혔다. 확인이 안 되면 `_pendingEvent`를 유지해 다음 턴에 재주입하되, 3번 시도해도 계속 무시되면 포기하고 넘어가 서사가 무한정 한 사건에 묶이지 않게 했다.

**해결결과**: v323 반영. 시도 횟수(`tries`) 카운터가 정상 누적되는 것을 코드 검토로 확인.

---

### P1-4. time 리셋을 값이 실제로 바뀐 턴에만

**원인**: `if (s.time) { gameState.time = s.time; resetTimeSlot(); }` — 모델이 준 시간 값이 **이전 값과 같은지 다른지 비교하지 않았다**(바로 위 location 처리는 비교를 했다). 모델이 같은 시간대 문자열을 그대로 재기입하기만 해도 시간 정체 카운터(`_slotTurns`)가 0으로 리셋되고, 백스톱이 요구하는 유일한 증거인 `_slotMoved`(장소 이동 여부)까지 함께 지워졌다.

**해결방법**: `String(s.time).trim() !== String(gameState.time||'').trim()`일 때만 갱신+리셋하도록 값 비교 추가.

**해결결과**: v320 반영. 재검증 과정에서 `_slotMoved`가 "리셋되면 영구히 지워지는" sticky 버그를 추가로 발견해 v322에서 별도 수정(아래 HIGH 참고).

---

### P1-5. time 표기 정규화 + 파싱 실패 시 무동작

**원인**: 아카데미 `initialState.time`이 `'입학식 아침'`으로 시작하는데, `timeFlowNudge`·`advanceTimeSlotIfStale`·경과일 주입 세 장치가 전부 `/\d+\s*일차/` 정규식에 의존한다. 즉 **1턴째부터 시간 관련 장치가 전부 즉시 return하며 죽어 있었다** — 유저가 겪은 "방에서 얘기하다 갑자기 아침" 증상은 실은 앱이 아니라 모델이 스스로 시간을 넘긴 것이었다(앱은 애초에 관여를 못 하고 있었다).

**해결방법**: 시작 시간을 `'1일차 아침 (입학식)'`로 정규식이 읽는 형식으로 변경. 시간대 파싱을 화이트리스트(아침|오전|오후|저녁|밤)+별칭(새벽→아침, 심야→밤 등)으로 바꾸고, 그래도 못 읽으면 '오전'으로 추측하지 않고 `null`을 반환하도록 수정(전엔 '늦은 밤' 같은 표기에서 시간이 거꾸로 갔다).

**해결결과**: v320 반영, 코드 확인으로 '새벽' 등 별칭 6종이 실제로 들어간 것을 검증.

---

### P1-6. choices 규칙 단일화 [부분완료]

**원인**: 2325행 "choices를 절대 생략하지 마라. 빈 배열·null도 금지"와 2383·2447행 "전투·판정 장면에서는 반드시 빈 배열"이 정면충돌. 각각 다른 시점의 유저 지적(2026-07-28, 2026-08-16)에 대응해 추가된 조항인데, 서로를 무효화하고 있었다. `PRIMING_SHOTS`(few-shot 예시)의 assistant 응답도 `choices: []`로 끝나 — 프롬프트 맨 앞의 예시가 "생략 금지" 규칙이 금지한 행동을 시연하고 있었다.

**해결방법**: 2325행을 "판정이 걸린 장면(전투·충돌·추격)이 아니면 최소 2개, 판정 장면에는 빈 배열"로 통합하고 "이 항목이 유일한 기준"을 명시. `PRIMING_SHOTS`의 `choices: []` 예시를 실제 선택지 2개로 교체.

**해결결과**: v321 완료. **미완**: 보고서가 "더 확실한 형태"로 제안한 `state_update.combat: true/false` 구조화(앱이 전투 턴을 직접 인지해 폴백 판단을 프롬프트 준수에만 맡기지 않는 방식)는 선택적 개선안이라 이번 라운드에서 보류. 문구 단일화만으로 정면충돌 자체는 해소됐다.

---

### P2-7. chronicle 수신 배선

**원인**: 출력 스키마가 매 턴 `chronicle` 배열을 요구하고 규칙문은 "이것이 장기 기억이다. 기록 안 하면 소멸한다"고 명시하는데, `processResponse`에 `data.chronicle`을 읽는 코드가 **없었다**. 모델이 매 턴 이 필드를 채우느라 토큰을 쓰는데 결과는 전량 폐기되고 있었다. 유일한 기록 경로는 별도 요약 패스(8~25턴에 한 번, "확정 사건"만 추출)뿐이라 유저가 대화로 정한 설정(이름·과거·약속 등)은 애초에 뽑히지도 않았다.

**해결방법**: `processResponse`의 state_update 처리 직후 `data.chronicle` 수신 로직 추가 — 문자열 항목만, 정규화 기준 중복 제거, 상한 유지, `replaying` 가드(되감기 재생 시 중복 방지).

**해결결과**: v321 반영, 코드 확인 완료.

---

### P2-8. 요약 트리거의 자기참조 제거

**원인**: 요약 발동 조건이 `unsummarized < SUMMARY_INTERVAL + _win`인데 `_win`(=`recentWindowCount()`)이 원문 총량이 예산(12만 자) 미만이면 `gameHistory.length`를 그대로 반환한다. 대입하면 `len - summaryIndex < 8 + len` → `summaryIndex > -8` → **summaryIndex가 0 이상인 한 항상 참**이라 무조건 스킵. 실측: `gameHistory` 120개(약 60턴)까지 요약이 단 한 번도 발화하지 않았다. 부수효과로 `trimHistory`도 `summaryIndex=0`이라 0을 반환해 `HISTORY_MAX(60)`가 사문이 되고, 원문이 무제한으로 불어났다(세이브 200~330KB의 몸통).

**해결방법**: 전송 창(문자 예산)과 요약 경계(턴 수)를 분리 — `SUMMARY_KEEP=16` 상수를 도입해 요약 가드와 `toSummarize` 슬라이스를 이 고정값 기준으로 변경.

**해결결과**: v321 반영. 재계산 시뮬레이션 — 이전엔 120개까지 스킵되던 것이 이제 **24개(약 12턴)**부터 요약 실행. `trimHistory`도 함께 살아남을 것을 확인(이후 P0-1 순서결함 수정으로 완전히 정상화).

---

### P2-9. 2부(개전) 전환의 결정론화

**원인**: `eventDueTurn`이 '학원제 직후'·'개전 직후' 같은 체인형(선행 사건에 상대적인) 조건에 전부 null을 반환해 모델 판단에만 맡겨져 있었다. 반면 숫자 조건 사건 5건(서열전·소환장·학원제·세드릭·이졸데)은 앱이 "이번 턴에 실제로 일으켜라"는 강제 블록을 받는다 — **정작 가장 중요한 전환점(개전)이 가장 약한 강제력**을 갖는 역전이 있었다. 세드릭 사건(cond `'입학 4~5개월 (개전 전후)'`)은 숫자 매치로 due=60이 잡혀 강제 발동하는데, 그 전제인 '개전'이 실제로 안 일어난 채로 "개전 전후"라는 문구가 60턴에 그대로 주입될 수 있었다.

**해결방법**: `eventDueTurn`이 이제 `{ due, chain }` 객체를 반환. cond 문자열에 다른 이벤트의 제목(키워드)이 포함돼 있고 그 이벤트가 이미 발동됐으면, 발동 턴+2로 체인을 잇는다(`chain:true` 표시). 체인 이벤트는 "직후"라는 의미상 월 단위 최소 간격 게이트(8턴)도 면제.

**해결결과**: 1~110턴 순차 플레이 시뮬레이션으로 9개 아카데미 이벤트 전부가 의도된 순서(1→9→17→30→45→**47(개전, 체인)**→**49(완충령의 선택, 체인)**→60→90턴)로 정상 발동하는 것을 확인. 개전이 세드릭·이졸데보다 먼저 온다.

---

## 4장 CRITICAL — ENGINE_MOD_CG 오배송

**원인**: `ENGINE_MOD_CG`(실측 10,240자)는 lovediary를 제외한 **모든 스토리**(academy 포함)에 무조건 주입됐다. 그런데 내부를 뜯어보니 이 중 9,072자(88%)가 「무대 뒤 3분」(idol) 전용 규칙 — 육성sim 필드, 주간일보, 광고계약, 업계인맥, 기자 로비 — 이었고 실제로 범용인 부분은 "이벤트 CG 규칙"(1,167자)뿐이었다. 단순 낭비를 넘어 모순까지 만들었다: "숫자는 절대 네가 만들지 마라"(idol 규칙)와 "스탯 초기값을 부여하라"(RPG 모듈)가 academy에서 정면충돌했다.

**해결방법**: `ENGINE_MOD_CG`를 `ENGINE_MOD_IDOL`(9,072자, idol 전용)과 `ENGINE_MOD_CG`(1,167자, 공통 CG 표시 규칙)로 분리. 주입 조건을 `if (_sidNow === 'idol') worldPrompt += ENGINE_MOD_IDOL;`로 변경해 idol에만 실리게 했다.

**해결결과**: v324 반영 — 이 분리 작업 도중 닫는 백틱을 빼먹어 파일 전체 문법이 깨지는 사고가 있었으나 `node --check`가 즉시 검출해 같은 턴에 수정. 최종 확인: `ENGINE_MOD_IDOL` 9,072자 / `ENGINE_MOD_CG` 1,167자로 정확히 분리, 아카데미는 이제 9,072자를 안 받는다.

---

## 4장 HIGH (9건)

### 1. _slotMoved sticky (재검증 중 발견)

**원인**: 장소 이동 시 `_slotMoved=true`가 세워지는데, 이걸 지우는 `consumeSlotMove()`는 백스톱이 실제로 발동할 때만 호출됐다. 즉 오래전의 장소 이동 증거가 다음 "시간 정체 에피소드"까지 그대로 넘어가, 방금 방에 들어와 10턴 넘게 대화만 해도(장소 이동 없이) **몇 턴 전의 낡은 증거로 백스톱이 발동**해 v315가 고치려던 "대화 중 아침" 증상이 다른 경로로 재발할 수 있었다.

**해결방법**: 시간이 모델에 의해 실제로 갱신되는 시점(`s.time`이 실제로 바뀌었을 때)에도 `consumeSlotMove()`를 호출하도록 추가 — 새 시간 에피소드가 시작되는 순간 낡은 이동 증거를 함께 소멸시킨다.

**해결결과**: v322 반영. 코드 검토로 호출 지점 확인.

### 2. 친밀도 사다리 3중 주입 충돌

**원인**: 친밀도 구간 판정이 세 곳에 흩어져 서로 다른 경계를 썼다 — 규칙문(0-20/21-40/41-60/61-80/81+), 상태 프롬프트 블록(<40/40-69/70+), 관계 단계 상태머신(≤20/≤45/≤70/70+). 친밀도 42일 때 두 곳은 "신뢰·비밀 일부 공유"라고 판정하는데 상태머신만 "비밀·상처는 굳게 닫혀 있다"고 상반되게 판정했다.

**해결방법**: 세 곳 모두 20/40/70 경계로 통일(상태머신의 45를 40으로 변경, 규칙문도 41~70/71+로 정렬).

**해결결과**: v325(1차 시도는 스크립트 크래시로 유실 → 재검증 중 발견 → Edit 도구로 재적용, v327 이전 grep으로 즉시 검증).

### 3. 대필금지 vs 독백허용 충돌

**원인**: "주인공 대필 절대 금지(위반=실격)"가 예외 없이 모든 대사·생각·감정·결정·행동을 금지하는데, 빙의 설정상 원작 지식은 "오직 주인공의 속마음·독백에서만" 표현되도록 되어 있어 두 규칙이 충돌했다.

**해결방법**: 대필금지 조항에 "관찰·회상·이미 아는 원작 지식의 인출은 대필이 아니다. 금지되는 건 새로운 결정·감정·대사를 지어내는 것뿐" 예외를 명시.

**해결결과**: 동일 경위(1차 유실 → 재검증 발견 → Edit 도구 재적용 → grep 검증)로 v327 최종 반영.

### 4. v317 능동성 규칙 vs 무명 원칙 충돌

**원인**: 이번 세션에서 새로 추가한 "인물은 먼저 말을 건다"(v317, NPC 능동성)가 아카데미 고유의 "노출 전엔 누구도 망나니에게 관심이 없다"(무명 원칙)와 정면충돌했다.

**해결방법**: v317 규칙에 "무명 원칙이 있는 세계관에서는 그 원칙이 우선한다 — NPC끼리의 대화·용무·소문으로 세계가 움직이고 있음을 보여줘라"는 예외를 추가.

**해결결과**: v325 반영, grep 검증 완료.

### 5. restoreSnap 되감기가 dialogueLog에 중복 기록

**원인**: 되감기(restoreSnap) 화면 재구성용 `showDialogue()` 호출이 `replaying` 플래그 없이 실행돼, `dialogueLog`에서 이미 뽑아온 마지막 대사를 `enqueueLine`이 다시 정상 기록 경로로 취급해 **되감기 1회당 같은 대사가 세이브에 영구 중복 저장**됐다.

**해결방법**: 해당 `showDialogue()` 호출을 `replaying=true`로 감싸고 호출 후 원래 값으로 복원.

**해결결과**: v325 반영.

### 6. 기록 모드가 아직 재생 안 된 대사를 스포일러로 노출

**원인**: `dialogueLog`는 v306부터 "재생된 만큼"이 아니라 "응답을 받은 만큼" 쌓인다. 그런데 기록넘김(◀)의 상한은 여전히 배열 길이 그대로였다 — 스트리밍 도중 ◀를 누르면 아직 화면에 한 번도 안 나온 대사를 먼저 읽어버리는 스포일러가 생겼다.

**해결방법**: 각 기록 항목에 `pending` 플래그를 부여(수신 시 true, 실제 표시 시 false). `lastNonPendingIndex()` 헬퍼로 기록넘김의 진입·전진 상한을 "실제로 화면에 나온 마지막 줄"로 제한.

**해결결과**: 노드 시뮬레이션 — 3줄 수신 중 2줄만 표시된 상태에서 ◀ 진입 시 `historyPos`가 정확히 2번째 줄에서 멈추고, ▶로 전진해도 3번째(미표시) 줄을 넘어가지 못하는 것을 확인. 3번째 줄이 표시된 뒤엔 정상적으로 도달 가능.

### 7. storySummary maxOutputTokens 부족 + 잘림 미검증 커밋

**원인**: 요약 시스템 문구는 "1000자 이내"를 요구하는데 Gemini 직접 경로의 `maxOutputTokens`는 600이었다 — 한국어 기준 1000자를 담기엔 부족해 응답이 중간에 잘렸고, `finishReason`을 확인하지 않고 잘린 텍스트를 그대로 `storySummary`로 확정했다. 다음 회차는 그 잘린 텍스트를 "기존 요약"으로 다시 압축하므로 손실이 매 요약마다 복리로 쌓였다.

**해결방법**: 상한을 600 → 1400으로 상향. `finishReason === 'MAX_TOKENS'`면 커밋하지 않고 기존 `storySummary`·`summaryIndex`를 유지한 채 이번 회차를 건너뛰도록 변경.

**해결결과**: v325 반영, 코드 확인.

### 8. IndexedDB 쓰기 실패가 완전 침묵

**원인**: `_bulkPutIDB`가 `.put()`이 던지는 동기 예외만 잡고 있었는데, IndexedDB의 쿼터 초과·트랜잭션 중단은 동기 예외가 아니라 request의 `onerror` 이벤트로만 통지된다 — 그걸 안 달아서 저장 실패가 완전히 침묵했고, `bulkSetSilent`는 IDB 모드에서 무조건 `true`를 반환해 성공한 줄 알고 있었다. 새로고침 후에야 진행이 사라진 게 드러나는 사고 유형.

**해결방법**: `req.onerror` 핸들러를 추가해 실패 시 콘솔 경고 + 토스트 알림.

**해결결과**: v325 반영, grep으로 핸들러 존재 확인.

### 9. 슬롯 불러오기가 이전 세션 summaryIndex로 새 세이브 히스토리 손상 (데이터손상급)

**원인**: `restoreAndReplay()`가 `gameHistory = save.gameHistory` 대입 직후 곧바로 `trimHistory(HISTORY_MAX)`를 실행하는데, 이 시점의 전역 `summaryIndex`는 아직 **이 세이브의 것이 아니라 이전 세션/이전 스토리에서 남은 값**이었다(`save.summaryIndex` 대입은 20여 줄 뒤에야 일어남). 70턴 이상 플레이한 뒤 다른 슬롯을 불러오면, 불러온 세이브의 히스토리 앞부분이 이전 판의 summaryIndex만큼 조용히 삭제되고 어디에도 복구 흔적이 안 남았다.

**해결방법**: 트림 블록을 `summaryIndex` 복원 뒤로 이동 — 세이브의 스칼라 값을 전부 먼저 복원한 다음 트리밍하도록 순서 변경.

**해결결과**: v322 반영. 시뮬레이션 — 오염된 전역 summaryIndex=40 상태에서 새 세이브(70개 항목, summaryIndex=5) 로드 시 이전엔 40 기준으로 잘렸을 것이 이제 세이브 자신의 값(5) 기준으로 정상 처리됨을 확인.

---

## 4장 MEDIUM (17건, 15건 완료 + 2건 다른 항목에서 동시 해소)

### 1. 반 배정 이벤트가 최소 간격 필터에 밀려 중복/영구폐기

**원인**: '입학식'과 '반 배정'이 둘 다 due=1이라 간격 게이트(`EVENT_MIN_GAP=8`)에 걸려 반 배정이 최대 8턴 늦게 열리거나, 시작 이벤트 제외 규칙(`due<=2 && tc>10`)에 걸려 영영 후보에서 빠질 수 있었다.

**해결방법**: `dueEventNow()`에서 후보 중 가장 이른 사건의 due가 2 이하(=시작 이벤트 클러스터)면 간격 게이트 자체를 건너뛰도록 수정.

**해결결과**: v326 반영, 순차 플레이 시뮬에서 1턴→2턴으로 정상 연속 발동 확인(P2-9 검증에 포함).

### 2. 요일·일과가 프롬프트에 전혀 안 실림

**원인**: LORE에 "요일별 담당 교수" 규칙은 있지만 일차→요일 계산 코드가 없어 모델이 매번 요일을 지어냈다(연애일기의 `ldNowClass` 패턴이 아카데미엔 미이식).

**해결방법**: 1일차=월요일로 앙커해 요일을 계산하고, 그날의 담당 교수·과목을 매 턴 명시하는 academy 전용 블록 추가.

**해결결과**: v326 반영, grep 검증.

### 3. 정적/동적 이벤트 트리 공존 모순

**원인**: 정적 이벤트 트리 규칙("조건이 안 됐으면 억지로 발생시키지 말 것")과 동적 "지금 일어날 사건" 강제 주입 블록이 매 턴 공존하며 서로를 부정했다.

**해결방법**: 정적 규칙에 "단 '지금 일어날 사건' 블록이 별도로 와 있으면 그쪽이 우선"이라는 예외 명시.

**해결결과**: v326 반영.

### 4. 출력 JSON 스키마가 헤더 없이 산문 한복판에 박혀 있음

**원인**: 형식 지시 문단 바로 뒤에 아무 설명 없이 JSON 스키마 블록이 시작돼, 모델이 "출력 형식이 시작됐다"는 걸 놓치기 쉬운 구조였다.

**해결방법**: "## ★출력 형식(JSON) — 아래 구조 그대로" 헤더 추가.

**해결결과**: v326 반영.

### 5. CG 씬키 목록 1,200자 이중 포함

**원인**: `ENGINE_MOD_CG`에 하드코딩된 전체 씬키 목록(약 1,200자)과, 스토리별 `CG_MANIFEST`로 검증된 동적 `cgBlock`의 씬키 목록이 **같은 턴에 둘 다** 실렸다 — 아카데미가 실제로 허용 안 하는 키까지 "유효한 것"처럼 뒤섞여 보일 위험이 있었다.

**해결방법**: 정적 하드코딩 목록을 제거하고, cg 필드 사용법 설명만 남긴 뒤 "유효 씬키는 별도 제공되는 목록을 따르라"고 참조하도록 변경.

**해결결과**: v326 반영, 문법 검사 통과.

### 6. `_isNarrationEcho`가 정당한 캐릭터 대사를 삼킴

**원인**: narration 문장과 우연히 겹치는 dialogue 항목은 화자·char_id·스프라이트 정보가 있어도 통째로 삭제됐다.

**해결방법**: `hasCharId` 매개변수 추가 — char_id가 붙은 대사는 이 필터를 아예 적용하지 않음.

**해결결과**: v326 반영. 시뮬레이션 5개 케이스 전부 통과(겹치는 문장이라도 char_id 있으면 보존, char_id 없으면 기존대로 차단).

### 7. `showDialogue`가 큐/보류분을 덮어쓸 때 `dropLogEntry` 미회수

**원인**: `dialogueQueue = []`로 큐를 비우거나 `window._pendingDialogues`를 덮어쓸 때 기존 항목을 `dropLogEntry`로 회수하지 않아, 화면엔 없는데 `dialogueLog`엔 `pending:true`로 영구히 남는 유령 항목이 생겼다.

**해결방법**: 큐 폐기 전 `dialogueQueue.forEach(dropLogEntry)` 추가. `_pendingDialogues`는 덮어쓰기 대신 `concat`으로 이어붙이도록 변경.

**해결결과**: v326 반영.

### 8. `_turnLineKeys` 차단 임계가 화자명 길이에 좌우

**원인**: 재투입 차단 임계(`_tk.length > 11`)가 화자명+구분자까지 합친 길이였다 — "학장 모르가나|응." 같은 실제로는 두 글자짜리 정당한 반복이 화자명 길이 때문에 차단 대상이 되는 비일관성이 있었다.

**해결방법**: 임계 판정을 본문 길이(`_echoNorm(d.text).length`)만으로 변경.

**해결결과**: v326 반영.

### 9. `_lastShownKey`가 턴/세이브 경계에서 리셋 안 됨

**원인**: 직전에 표시된 줄과 새 턴 첫 줄이 우연히 같은 텍스트면, 정당한 새 줄이 "직전과 동일"로 오인돼 삭제될 수 있었다. 세이브 로드 시에도 리셋이 안 됐다.

**해결방법**: `resetTurnLineKeys()`가 호출되는 턴 시작 지점(스트림·비스트림) 2곳과 `restoreAndReplay()`(세이브 로드) 진입부에 `window._lastShownKey = null` 추가.

**해결결과**: v326 반영.

### 10. 연대기 추출이 요약 구간의 마지막 4000자만 봄

**원인**: 요약 대상(`newContent`)이 4000자를 넘으면(흔함, 8~25천자대) 앞쪽 사건은 연대기 추출 대상에서 아예 빠졌다.

**해결방법**: `newContent.slice(-4000)` → 전체 `newContent`를 그대로 사용.

**해결결과**: v326 반영.

### 11. 연대기 60개 저장 / 30개만 프롬프트 주입 불일치

**원인**: 저장 상한(60)과 주입 상한(30)이 달라, 원문 히스토리에서 이미 밀려나 연대기가 유일한 근거인 오래된 사실부터 프롬프트에서 사라졌다.

**해결방법**: 주입 상한을 저장 상한과 동일하게 60으로 통일.

**해결결과**: v326 반영.

### 12. metChars(등장인물 상태) 무제한 주입

**원인**: 만난 인물 전원의 관계·친밀도·상태·경지·관계단계가 인원 제한 없이 매 턴 전량 주입됐다. 오래 플레이할수록 지금 장면에 없는 인물까지 연대기보다 많은 지면을 차지했다.

**해결방법**: 친밀도 상위 12명(`META_FULL_CAP`)만 전체 상세를, 나머지는 "이름(친밀도)" 한 줄로 압축.

**해결결과**: v326 반영. 시뮬레이션(15명 중 상위 12명 전체상세/하위 3명 압축)으로 계층화 로직 검증. ★부분완료 → 완료 정정: 초기 구현에서 압축 목록(`_demoted`)을 계산만 하고 실제로 프롬프트에 출력하지 않는 결함을 재검증 중 발견 — 강등된 인물이 프롬프트에서 완전히 사라져 모델이 아예 모르는 사람처럼 취급할 위험이 있었다. 즉시 "그 외 만난 적 있는 인물" 압축 줄 출력을 추가해 완결.

### 13. 스냅샷 60개가 gameState 전체를 통째 복사해 고용량

**원인**: 되감기 스냅샷(`captureSnap`)은 `gameHistory`·`dialogueLog`는 길이만 남기도록 이미 최적화됐지만, `gameState`(metChars·chronicle 포함)와 `storySummary`는 매번 전체를 복사했다. 스냅샷 1개당 약 8~12KB, 60개면 500~700KB로 실제 세이브(200~330KB)보다 컸다.

**해결방법**: 안전하게 줄일 수 있는 손잡이가 개수뿐이라 `SNAP_MAX`를 60 → 30으로 축소.

**해결결과**: v326 반영.

### 14. `knows` 필드가 죽어 있음

**원인**: 프롬프트는 매 턴 이 값을 읽어 "이 인물은 이것만 안다"는 극적 아이러니를 강제하는데, 정작 모델이 이걸 써 보낼 스키마·병합 경로가 없었고 세이브 로드마다(구 감독 잔재 청소에 함께 딸려) 삭제됐다.

**해결방법**: `s.chars` 병합 로직에 `knows: u.knows ?? cur.knows` 추가, 출력 스키마에 `knows` 필드 설명 추가, 세이브 로드 시 삭제 목록에서 제외(감독 시절 오염원이었던 `arc`만 계속 삭제).

**해결결과**: v326 반영, grep 검증.

### 15. 정적 규칙 총량 축소

**원인**: 정적 규칙 총량이 설계 문서 추정치의 2배 이상(약 77,000자)이었다.

**해결방법**: 별도 조치라기보다 CRITICAL(MOD_IDOL 분리, -9,072자)과 CG 씬키 중복 제거(-1,200자)가 직접적인 축소 효과를 냈다.

**해결결과**: 실질 해결 — 두 항목 반영으로 아카데미 기준 최소 10,272자 감소.

### 16~17. 이벤트체인 null / 저장실패 침묵 (원문 자체가 다른 항목과 동일 결함으로 표기)

**원인·해결**: 원문 보고서가 스스로 "P2-9와 동일 결함", "HIGH IndexedDB 항목과 동일 뿌리"라고 명시한 항목들. P2-9(체인 해소)와 HIGH의 IndexedDB `onerror` 처리로 각각 함께 해소됨.

**해결결과**: 별도 조치 불필요, 상위 항목 처리로 완료.

---

## 4장 LOW (8건)

### 1. 일차/개월 환산 스케일 불일치

**원인**: 보고서 작성 시점엔 시간대 넛지가 "하루≈3턴 강제 진행"(v315 구조)이었는데 `TURNS_PER_MONTH=15`(1개월=15턴)라 두 시계가 어긋난다는 지적이었다.

**해결방법 및 결과**: 이미 v316(TURNS_PER_MONTH 도입)과 v318(강제 진행 폐기, 넛지+판단위임 방식으로 재설계)로 구조 자체가 바뀌어 있었다. 지금은 "사건 발동은 턴 수 기준, 화면에 표시되는 일차는 모델 재량"이 의도된 설계이고 둘을 강제로 동기화하지 않는다 — 이벤트 주입문에 이미 "서사 시간과 안 맞으면 경과를 요약하고 시간을 건너뛰라"는 브리징 지시가 포함돼 있어 드리프트를 흡수한다. 코드 검토로 확인, 추가 조치 불필요.

### 2. `_timeSkipNote`가 장면 상태를 무시

**원인**: 앱을 닫았다 돌아왔을 때의 "시간 경과" 지시가 무조건적("반드시 반영")이라, NPC가 막 질문을 던진 대화 한복판에 앱을 닫았다 돌아와도 대사 도중에 갑자기 몇 시간이 건너뛸 수 있었다.

**해결방법**: 직전 장면이 대화·사건 중간에 끊긴 상태였으면 그 장면을 먼저 자연스럽게 마무리한 뒤에 시간 경과를 반영하라는 조건부 지시로 변경(이미 매듭지어진 상태였으면 바로 반영해도 됨).

**해결결과**: v327 반영.

### 3. background 예시 키가 특정 스토리(무협) 전용

**원인**: 배경 필드 설명에 하드코딩된 예시("저잣거리=market_street, 객잔/주막=inn_hall" 등)가 무협 스토리 전용 키였다 — "목록에서만 골라라"고 하면서 정작 그 목록에 없는 키 4개를 예시로 제시하는 모순.

**해결방법**: 예시를 하드코딩하지 않고 그 스토리의 실제 `bgKeys` 목록에서 앞 3개를 동적으로 뽑아 예시로 사용.

**해결결과**: v327 반영.

### 4. "변화 없는 필드는 넣지 말 것" vs 강제 채움 지시 충돌

**원인**: 이 규칙이 choices(매 응답 필수)·cg(장면 진행 중 매 턴 필수)에도 적용되는 것처럼 읽혀, 되쓰기·미채움 유인이 됐다(P1-4의 time 되쓰기 버그와 같은 뿌리).

**해결방법**: 이 규칙은 `state_update` 안의 개별 값(location·status 등)에 대한 것이며, "매 턴 채워라"라고 별도로 명시된 필드(choices·cg)에는 적용하지 않는다고 명시.

**해결결과**: v327 반영.

### 5. 스트리밍 중 배경(`lastBg`)이 정정되지 않음

**원인**: `lastBg`(세이브·복원에 쓰이는 전역 변수)는 비스트리밍 완료 경로에서만 갱신됐다. 실제로 흔한 스트리밍 경로에서는 화면(`currentBgFile`)은 바로 바뀌어도 `lastBg`는 갱신 안 돼, 세이브의 `lastBg` 필드가 몇 턴 전 배경에 멈춰 있다가 이어하기·되돌리기 첫 화면에 옛 배경이 잠깐 보일 수 있었다.

**해결방법**: `setBg()` 함수 내부에서 `currentBgFile`을 갱신하는 바로 그 지점에 `lastBg`도 함께 동기화.

**해결결과**: v327 반영.

### 6. iOS Safari 16.4 미만 lookbehind 정규식 파싱 실패 (잠재 CRITICAL)

**원인**: `_isNarrationEcho`(v313에서 내가 추가)의 문장 분리 정규식이 lookbehind(`(?<=...)`)를 사용했다. 이 문법은 Safari 16.4(2023년 3월) 이전엔 지원되지 않는데, JS 엔진은 **지원 안 하는 정규식 리터럴이 파일 안에 있으면 그 정규식을 호출하는지 여부와 무관하게 `<script>` 블록 전체를 파싱 단계에서 거부**한다 — 구형 iOS 사용자에게는 앱이 통째로 흰 화면이 될 수 있는, 이번 감사에서 발견된 것 중 블라스트 반경이 가장 큰 잠재 결함이었다.

**해결방법**: 문장부호 뒤에 구분자(제어문자 하나)를 심고 그 구분자로 split하는 방식으로 lookbehind 없이 동일한 분리 효과를 냄.

**해결결과**: 수정 과정에서 타이핑으로 `\n` 이스케이프 시퀀스가 반복적으로 실제 개행문자로 깨지는 사고가 3차례 있었고, 매번 `node --check`가 즉시 검출 — 최종적으로 `chr(92)` 코드 조합 기법으로 정확히 수정. grep으로 정규식 리터럴 형태의 lookbehind 0건 확인, 노드 시뮬레이션 5개 케이스(char_id 가드 포함) 전부 통과.

### 7. `director`·`day` 죽은 필드

**원인**: 총감독 시스템 폐지(v297) 이후 쓰기 경로가 없는 죽은 필드가 세이브에 남아있다는 지적.

**해결방법 및 결과**: 확인 결과 `director`는 이미 세이브 로드 시 `null` 처리하는 방어 코드 1줄뿐이라 실질적 위험이 없고, `day` 필드는 현재 코드베이스에 존재하지 않는다(보고서 작성 시점 이후 이미 정리된 것으로 판단). 추가 조치 불필요.

### 8. 첫 요약 라벨링 이슈

**원인**: 프롤로그 몇 턴만 요약한 첫 요약도 "[지금까지의 이야기 요약]"이라는 동일 라벨로 최상단에 실려, 이야기 초반부터 다소 어색하게 노출될 수 있었다.

**해결방법 및 결과**: 별도 조치 없이 P2-8(요약 트리거를 60~70턴대에서 12턴대로 앞당김)로 실질 해소 — 첫 요약이 훨씬 이른 시점에 훨씬 적은 분량으로 발생해 위화감이 줄었다.

---

## 미완료 2건 (의도적 보류)

### P1-6 잔여 — `state_update.combat` 필드 구조화
choices 규칙의 정면충돌 자체는 문구 통합으로 이미 해소됨(완료). 보고서가 "더 확실한 형태"로 제안한, 앱이 전투 여부를 별도 구조화 필드로 직접 받아 판단하는 방식은 스키마 확장 + 병합 로직 추가가 필요한 별도 기능 추가에 가까워 이번 라운드에서는 보류.

### 중복 규칙 5종 중 4종 (CG 씬키 제외)
세계 능동성(3곳)·복선(2곳)·완급 리듬(2곳)·비밀 단계(4곳)·이름 창작 금지(3곳, 그중 1곳 반대 지시) 중 CG 씬키 이중 포함(가장 구체적이고 안전하게 고칠 수 있던 것)만 이번 라운드에서 제거. 나머지는 각각 2~4곳에 산재해 있어 안전하게 통합하려면 각 위치의 뉘앙스 차이를 전부 대조해야 하는 작업량이 크다 — 임의로 서둘러 통합했다가 뉘앙스 손실로 새 결함을 만들 위험이 있어 보류.

---

## 작업 중 발견한 프로세스 사고 (투명성을 위해 기록)

1. **스크립트 중도 크래시로 인한 변경 유실**: 배치 스크립트 하나가 여러 치환을 순차 실행하다 4번째 항목에서 인코딩 문제로 크래시. 파일 쓰기(`io.open().write()`)가 모든 치환 완료 후 마지막에 한 번 실행되는 구조였는데, 크래시로 그 지점에 도달하지 못해 **먼저 성공한 3개 치환도(콘솔에 "OK" 출력까지 됐음에도) 전부 디스크에 저장되지 않았다**. 재검증 단계에서 grep으로 실제 파일 상태를 직접 확인하다가 발견 — 두 항목(친밀도 경계, 대필금지 예외)을 Edit 도구로 다시 적용했다. 이후 모든 스크립트 실행 직후 grep으로 즉시 검증하는 방식으로 전환.
2. **백슬래시 이스케이프 시퀀스 반복 손상**: `
`, `\n` 등의 리터럴 이스케이프 시퀀스가 heredoc을 통한 타이핑 과정에서 여러 차례 실제 제어문자로 깨졌다(구체적으로 lookbehind 제거 작업에서 3차례). `chr(92)` 등 코드로 문자를 조합하는 방식으로 우회해 해결.

두 사고 모두 최종 파일에는 반영되지 않았고(발견 즉시 수정), 라이브 배포본(v327)에는 영향이 없다.
