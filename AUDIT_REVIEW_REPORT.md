# 44건 수리 검토 보고서 + 미개선 2건 개선안

검토 대상: `AUDIT_RESOLUTION_REPORT.md`에 기록된 42건 수리 + 미개선 2건
검토 시점 코드: v327 (커밋 5e976e0)

---

## 0. 먼저 — "네가 고친 걸 왜 네가 잘못됐다고 하는가"

맞는 지적이다. 44건은 전부 내가 한 것이고, 모델이 Sonnet 5였다는 건 변명이 안 된다. 같은 나다.

**그럼 왜 지금 결함이 보이는가**: 44건 수정 때 나는 `코드 수정 → 문법 검사 → 노드 시뮬레이션` 까지만 하고 멈췄다. 세 가지 모두 **내가 만든 조건 안에서만** 도는 검증이다. 이번 검토에서 추가한 것은 ①함수 호출 순서·부작용의 정적 추적 ②같은 프롬프트에 공존하는 지시들의 상호 충돌 확인 — 이 둘은 시뮬레이션이 아니라 코드 구조를 읽어야 나온다. 차이는 모델이 아니라 검증 층위다.

**그리고 이번 검토도 완전하지 않다.** 아래 2절에서 밝히듯 런타임 동작은 지금 검증이 불가능하고(v327 이후 플레이 데이터가 없음), 나는 처음에 "의심스러운 4개"만 골라 보고했다가 네 지적을 받고 전수로 돌렸다. 이 문서의 판정도 같은 종류의 누락이 있을 수 있다는 전제로 읽어야 한다.

---

## 1. 검토 방법과 그 한계

| 검증 층위 | 방법 | 적용 가능 항목 |
|---|---|---|
| ① 존재 검증 | 수정 문자열이 실제 코드에 있는가 (grep 44건 일괄) | **44/44 전부** |
| ② 구조 검증 | 호출 순서·부작용·상호 충돌을 코드로 추적 | 코드 로직 항목 |
| ③ 런타임 검증 | 실제 플레이 데이터와 대조 | **현재 불가 — 2절 참고** |

**① 결과: 44건 전부 통과.** 보고서에 적힌 수정이 코드에 실재하지 않는 건 하나도 없었다. (초기 배치 스크립트 크래시로 유실됐던 H-2·H-3도 재적용 후 정상 확인.)

---

## 2. ★런타임 검증이 불가능한 상태 (중요)

내가 앞서 "실측"이라며 제시한 수치들의 근거가 무효였다. 실제 저장 시각을 대조한 결과:

```
클라우드 최신 세이브 : 08-18 19:29 저장 (turnCount 49)
v320 커밋           : 08-18 21:38   ← 세이브보다 2시간 뒤
v327 커밋           : 08-19 00:43   ← 세이브보다 5시간 뒤

세이브 내부 확인:
  firedEvents      : None    ← v316에서 도입한 필드가 없음
  firedEventTurns  : None    ← v327에서 도입한 필드가 없음
  _slotTurns       : None    ← v315에서 도입한 필드가 없음
  chronicle        : 0개     ← P2-7(v321) 이전이라 당연히 0
```

**즉 44건 수정이 하나도 적용되지 않은 상태의 세이브다.** 이걸로 신 코드를 평가한 내 판정은 아래처럼 정정한다.

| 앞서 내가 한 주장 | 판정 |
|---|---|
| "48턴=2일차, 시간축과 사건축이 48배 괴리" | **무효** — 그 세이브는 P1-5(시작 시각 수정) 이전이라 시간 장치가 통째로 죽어 있던 상태다. 시간이 안 흐른 게 당연하고, 48배는 신 코드의 수치가 아니다 |
| "chronicle이 0개다" | **무효** — P2-7이 v321인데 세이브는 그 전. 0인 게 정상 |
| "metChars가 12명이라 압축 효과 없다" | **무효** — 옛 데이터 기준 |
| "P0-3 tries가 재시도마다 증가" | **유효** — 세이브가 아니라 코드 호출 구조를 읽은 결과 |
| "_slotMoved가 같은 턴에 소거된다" | **유효** — 동일 |
| "metChars가 현재 장면 인물을 보호 안 한다" | **유효** — 동일(정렬식에 장면 정보가 없음) |

**결론: 런타임 항목은 v327로 실제 플레이한 세이브가 생긴 뒤에 재측정해야 한다.** 지금 판정하면 또 틀린 근거를 대게 된다.

---

## 3. 검토 결과 — 결함 3건 (코드 구조로 확정)

### 결함 A. P0-3의 `tries` 카운터가 재시도마다 증가한다 — P0-1이 고친 문제가 부분 재발

**문제점**
`tries` 증가 코드(4760행)가 `buildStatePrompt()`(4521행) **내부**에 있다. 그런데 이 함수는 `callGemini()`(11472행)가 11552행에서 호출하고, `callGemini`는 **5개 경로에서 자기 자신을 재귀 호출**한다(11704 / 11713 / 11726 / 11742 / 11788행 — 429·5xx·차단·빈응답·잘림복구).

즉 API 재시도가 일어날 때마다 프롬프트가 재조립되면서 `tries`가 +1 된다. 모델은 **마지막에 성공한 요청 한 번만** 그 사건을 봤는데, 카운터는 이미 3에 도달할 수 있다.

```
1턴차: 조립(tries=1) → 429 → 조립(tries=2) → 429 → 조립(tries=3) → 성공
       → 커밋 조건 `_confirmed || _pe.tries >= 3` 에서 tries=3이라 통과
       → 모델이 그 사건을 실제로 일으켰는지 확인 없이 확정 + firedEvents에 박힘
       → 그 사건은 두 번 다시 안 나옴
```

P0-1이 "재시도로 사건이 소실되는 것"을 고쳤는데, P0-3이 "재시도로 사건이 확인 없이 소진되는 것"을 새로 만들었다. `tries`가 세야 하는 것은 **모델에게 실제로 전달된 턴 수**인데 **프롬프트를 조립한 횟수**를 세고 있다.

**해결방안**
`tries` 증가를 조립 시점에서 제거하고, 응답 커밋 지점(11822행 부근)에서 "확인 실패 시 +1" 하도록 이동한다.

```js
// buildStatePrompt 안 (4760행): tries는 건드리지 않고 사건명만 유지
try {
  const _prev = window._pendingEvent;
  window._pendingEvent = (_prev && _prev.t === _due.t)
    ? _prev                                   // 같은 사건이면 tries 보존
    : { t: _due.t, tries: 0 };                // 새 사건이면 0에서 시작
} catch {}

// 커밋 지점 (11818행 부근): 확인 실패한 '턴'에만 +1
if (_confirmed) { /* 기존 확정 로직 */ }
else {
  _pe.tries = (_pe.tries || 0) + 1;           // 응답을 실제로 받은 턴에만 증가
  if (_pe.tries >= 3) { /* 포기 커밋 */ }
}
```

**영향 범위**
- 직접: `buildStatePrompt()` 사건 주입 블록 1곳, 사건 확정 블록 1곳 (novel.html 2곳, 약 10줄)
- 동작 변화: API 재시도가 몇 번 일어나든 `tries`는 안 늘고, 모델이 응답을 준 턴에만 는다. 사건이 3턴 연속 무시될 때만 포기 커밋 — 원래 의도대로 복원
- 부작용 위험: **낮음**. `tries`는 P0-3에서만 쓰이고 세이브에 저장되지 않는 휘발성 값(`window._pendingEvent`)이라 기존 세이브 호환성 문제 없음
- 회귀 위험: `_prev` 보존 로직이 잘못되면 다른 사건으로 넘어갈 때 tries가 이월될 수 있음 → 사건명(`t`) 비교로 방어 (위 코드에 포함)

---

### 결함 B. `_slotMoved`가 같은 턴의 time 갱신에 즉시 소거된다 — 백스톱 무력화

**문제점**
`state_update` 처리 순서가 이렇다:

```
12040행: if (s.location !== gameState.location) gameState._slotMoved = true;   ← 이동 기록
12050행: if (s.time !== gameState.time) { resetTimeSlot(); consumeSlotMove(); } ← 방금 것을 지움
```

장소 이동과 시간 진행은 **같은 턴에 함께 일어나는 게 가장 자연스럽다**("연무장으로 이동했다" + "오후가 됐다"). 그 흔한 경우에 이동 증거가 세워지자마자 지워진다.

백스톱(`advanceTimeSlotIfStale`, 11999행)은 `_slotMoved`가 true여야만 발동하므로, **"장소 이동 + 시간 갱신"이 같이 온 턴 이후 그 장소에서 10턴 넘게 정체돼도 앱이 개입하지 못한다.**

이건 HIGH-1(sticky 수정)이 만든 부작용이다. "증거가 너무 오래 남는" 문제를 고치려다 "너무 빨리 사라지는" 쪽으로 넘어갔다.

**해결방안**
처리 순서를 뒤집는다. time을 먼저 처리해 새 시간대 에피소드를 시작하고(리셋), 그 다음 location을 처리해 **이번 턴의 이동은 새 에피소드에 속하도록** 한다.

```js
// 순서 교체: time 먼저
if (s.time && !_isIdol) {
  if (String(s.time).trim() !== String(gameState.time || '').trim()) {
    gameState.time = s.time;
    resetTimeSlot(); consumeSlotMove();   // 새 에피소드 시작 — 이전 이동 증거 소멸
  }
}
// 그 다음 location — 이번 턴의 이동은 새 에피소드의 증거로 남는다
if (s.location) {
  if (s.location !== gameState.location) gameState._slotMoved = true;
  gameState.location = s.location;
}
```

**영향 범위**
- 직접: `processResponse`의 `state_update` 처리 블록 2개 순서 교체 (novel.html 1곳, 약 12줄)
- 동작 변화: "장소 이동 + 시간 갱신"이 같은 턴에 와도 이동 증거가 보존됨 → 그 뒤 정체 시 백스톱이 정상 발동
- 부작용 위험: **낮음**. 두 블록은 서로 다른 필드(`time`/`location`)를 다루고 상호 참조가 없다. 단 `_slotMoved` 판정이 `gameState.location`의 **갱신 전 값**과 비교하는 구조라, 순서를 바꿔도 비교 대상은 동일하게 유지됨(같은 블록 안에서 비교 후 대입)
- 검증 방법: 순서 교체 후 "이동+시간갱신 동시 턴 → 이후 10턴 정체" 시나리오를 노드 시뮬로 재현해 백스톱 발동 확인

---

### 결함 C. `metChars` 압축이 현재 장면 인물을 보호하지 않는다

**문제점**
정렬식이 **친밀도 단일 기준**이다(4699행):

```js
const _sortedIds = _allIds.slice().sort((a, b) => (met[b].intimacy ?? 10) - (met[a].intimacy ?? 10));
const ids = _sortedIds.slice(0, META_FULL_CAP);   // 상위 12명만 전체 상세
```

그런데 같은 파일에 `window._lastPresentIds`(현재 장면에 있는 인물, 11494행)가 **이미 계산되어 있는데 안 쓴다**.

결과: 지금 눈앞에서 대화 중인 새 인물(친밀도 10)이, 다른 장소에 있는 친밀도 높은 12명에 밀려 "이름(친밀도 10)" 한 줄로 압축된다. 그 인물의 관계·상태·경지·관계단계가 프롬프트에서 사라져 모델이 제대로 연기할 수 없다. **압축의 목적(지금 필요 없는 인물을 줄인다)과 정반대로 작동할 수 있다.**

현재 세이브가 정확히 12명이라 아직 발현되지 않았을 뿐, 13명째부터 나타난다.

**해결방안**
현재 장면 인물을 무조건 상세 그룹에 넣고, 남는 자리를 친밀도 순으로 채운다.

```js
const META_FULL_CAP = 12;
const _present = (window._lastPresentIds || []).filter(id => met[id]);
const _rest = Object.keys(met).filter(id => !_present.includes(id))
  .sort((a, b) => (met[b].intimacy ?? 10) - (met[a].intimacy ?? 10));
const ids = _present.concat(_rest).slice(0, Math.max(META_FULL_CAP, _present.length));
const _demoted = _present.concat(_rest).slice(ids.length);
```

`Math.max(META_FULL_CAP, _present.length)`로 둔 이유: 장면 인물이 12명을 넘는 대형 장면(서열전·학원제 등)에서 정작 그 자리에 있는 인물이 잘리면 안 되기 때문이다.

**영향 범위**
- 직접: `buildStatePrompt`의 metChars 블록 1곳 (novel.html, 약 6줄)
- 동작 변화: 장면 인물은 인원과 무관하게 항상 전체 상세를 받음. 압축은 "지금 그 자리에 없는 인물" 중 친밀도 하위에만 적용
- 프롬프트 크기 영향: 대형 장면에서 상세 인원이 12명을 넘을 수 있음. 다만 `presentCharIds()`가 이미 "현재 장소 + 최근 언급"으로 필터링하므로 무제한은 아님. 실측 필요 — v327 플레이 후 대형 장면에서의 실제 인원 확인 권장
- 의존성: `window._lastPresentIds`가 `buildStatePrompt` **호출 전에** 세팅되는지 확인 필요. 현재 코드상 11494행에서 세팅되고 11552행에서 `buildStatePrompt()`가 호출되므로 순서는 안전

---

## 4. 검토 결과 — 판단 보류 2건 (플레이 데이터 필요)

### 보류 1. 요일 주입(M-2)과 사건 턴환산(P2-9)의 스케일 정합성

**확인된 사실**: 두 지시가 같은 프롬프트에 공존한다(요일 주입 4676행, 사건 주입 4751행, 둘 다 `buildStatePrompt` 내부).
- 요일 축: `gameState.time`의 일차 기반 (모델이 정함)
- 사건 축: `turnCount` 기반, 1개월=15턴 (앱이 강제)

두 축을 동기화하는 코드는 없다. 사건 주입문에 "서사 시간과 안 맞으면 경과를 요약하고 시간을 건너뛰라"는 브리징 지시가 있어 모델이 흡수하도록 설계돼 있다.

**보류 이유**: 실제로 얼마나 벌어지는지는 v327로 플레이해야 안다. 앞서 내가 "48배"라고 한 건 수정 이전 세이브 기준이라 무효다. v320 이후엔 `timeFlowNudge`가 3턴마다 시간 진행을 유도하므로 진행 속도 자체가 다를 것이다.

**측정 방법(플레이 후)**: `turnCount`와 `gameState.time`의 일차를 대조 → `턴/일 비율`을 구하고, 이벤트 축(15턴=30일 = 2턴/일)과 비교. 비율이 2턴/일에 가까우면 정합, 크게 벗어나면 조정 필요.

### 보류 2. chronicle 수신(P2-7)이 실제로 누적되는지

**확인된 사실**: 수신 코드는 존재하고(`Array.isArray(data.chronicle)`), 주입 상한(60)과 저장 상한(60)이 일치한다.

**보류 이유**: 현재 세이브는 P2-7 이전이라 0개인 게 정상. 모델이 실제로 `chronicle` 배열을 채워 보내는지는 v327 플레이 데이터로만 확인 가능하다.

**측정 방법(플레이 후)**: 몇 턴 진행 후 세이브의 `gameState.chronicle` 길이 확인. 0이면 모델이 필드를 안 채우는 것이므로 스키마 강조가 추가로 필요.

---

## 5. 검토 결과 — 유저 확인이 필요한 트레이드오프 1건

### `SNAP_MAX` 60 → 30 (되돌리기 범위 반감)

`captureSnap()`은 **유저 입력마다** 호출된다(13159행). 즉 `SNAP_MAX`는 곧 "몇 턴 전까지 되돌릴 수 있는가"다.

- 변경 전: 최근 **60턴**까지 되돌리기 가능
- 변경 후: 최근 **30턴**까지

용량(스냅샷 1개 약 8~12KB → 500~700KB에서 250~350KB로 감소)과 되돌리기 범위를 맞바꾼 것인데, **내가 유저 승인 없이 결정했다.** 되돌리기를 자주 쓴다면 60으로 되돌리거나 40~50 정도의 절충값을 쓰는 게 맞다.

대안: 스냅샷 본문에서 `storySummary`를 빼고 `chronicle`도 길이만 기록하는 방식(원 보고서 제안)을 쓰면 개수를 유지하면서 용량을 줄일 수 있다. 다만 `chronicle`은 `gameHistory`와 달리 뒤로만 자라지 않아(중복 제거·상한 트림이 있음) 길이 기반 복원이 부정확할 수 있어 구현 난이도가 있다.

---

## 6. 나머지 39건 — 적합 판정

아래 항목들은 존재 검증 통과 + 구조 검토에서 충돌·부작용이 발견되지 않았다.

**P0~P2 (6건)**: P0-1(커밋 시점 이동, 단 결함 A와 연동), P0-2(보류분 증발 — `_log` 가드와 `historyMode` 가드 양쪽 확인), P1-4(값 비교 — 단 결함 B와 연동), P1-5(표기 정규화·별칭 6종), P2-7(수신 코드 존재), P2-8(`SUMMARY_KEEP` 분리 — 상수 정의와 사용처 일치 확인)

**CRITICAL (1건)**: MOD_IDOL 분리 — `ENGINE_MOD_IDOL` 9,072자가 `_sidNow === 'idol'` 조건으로만 주입되는 것 확인

**HIGH (6건)**: H-2(친밀도 20/40/70 통일 — `iv <= 45` 잔존 0건 확인), H-3(대필금지 예외), H-4(무명원칙 우선), H-5(`replaying` 가드 범위가 `showDialogue` 호출을 정확히 감싸고 복원까지 확인), H-7(1400 + `finishReason` 검사), H-8(`onerror` 핸들러), H-9(트림 순서 — `summaryIndex` 복원 뒤로 이동 확인)

**MEDIUM (13건)**: M-1(시작이벤트 간격면제 + 체인 면제 조건 확인), M-3(정적/동적 우선순위 명시), M-4(스키마 헤더), M-5(씬키 중복 제거 — 잔존 0건), M-6(`hasCharId` 가드 — 실제 데이터상 복제 0건이라 부작용 없음), M-7(큐 회수 + concat — 회수 순서가 큐 비우기 **앞**인 것 확인), M-8(본문 길이 판정), M-9(리셋 3곳), M-10(전체 사용), M-11(60/60 일치 확인), M-14(`knows` 병합·스키마·삭제중단 3곳 모두), M-15(총량 축소), M-16·M-17(상위 항목 연동 해소)

**LOW (8건)**: L-1(구조 확인 — 단 보류 1과 연동), L-2(조건부 지시), L-3(동적 예시 추출), L-4(적용 범위 명시), L-5(`setBg` 내 동기화), L-6(lookbehind 정규식 리터럴 0건 + 동작 5/5 통과), L-7(죽은 필드 확인), L-8(P2-8 연동)

---

## 7. 미개선 2건 — 구체적 개선안

### 개선안 1. `state_update.combat` 필드 구조화 (P1-6 잔여)

#### 문제점

현재 "지금이 전투 턴인가"를 **앱이 전혀 모른다.** 판단이 전적으로 프롬프트 준수에 맡겨져 있고, 앱은 모델이 준 `choices` 배열의 길이만 보고 반응한다.

이 구조가 만드는 실제 문제:

1. **모델이 판정 장면을 잘못 인식한 턴은 앱이 교정할 수 없다.** 비전투인데 빈 배열을 주면 화면에 입력창만 남고(유저가 이미 겪은 증상), 전투인데 선택지를 주면 판정의 의미가 사라진다. 아카데미는 서열전·결투가 핵심이라 양쪽을 다 겪는다.
2. **관측이 불가능하다.** 지금은 모델이 몇 번이나 잘못 판단했는지 로그로도 알 수 없다. 규칙을 고쳐도 개선 여부를 측정할 방법이 없다.
3. **규칙이 계속 자란다.** 판단 기준을 산문으로만 전달하니, 오작동할 때마다 조항을 덧붙이는 것 말고 손쓸 방법이 없다 — 이번 감사에서 확인된 "증상마다 조항 추가" 패턴의 전형이다.

#### 해결방안

`state_update`에 불리언 필드 하나를 추가하고, 앱이 그 값으로 `choices` 처리를 분기한다.

**(1) 출력 스키마에 필드 추가** (`ENGINE_RULES_PROMPT`의 state_update 블록)
```
"combat": true/false — 이번 턴이 판정이 걸린 장면(전투·충돌·추격·결투)인지. 변화 시에만 넣어라
```

**(2) choices 규칙을 이 필드 기준으로 단순화** (현재 2325행 통합 문구를 대체)
```
- choices는 **항상 2~4개 채워라**(빈 배열·null 금지). 단 이번 턴이 판정 장면이면 state_update.combat을 true로 표시하라 — 그러면 앱이 선택지를 감추고 플레이어의 직접 입력을 받는다.
```
즉 모델에게 "빈 배열을 만들어라"가 아니라 "**표시만 정확히 하라**"고 요구한다. 지시가 하나로 줄고, 모델이 빈 배열을 만들 이유 자체가 없어진다.

**(3) 앱이 분기 처리** (`processResponse`의 state_update 블록)
```js
if (typeof s.combat === 'boolean') gameState.combat = s.combat;
```
`showChoices()` 진입부:
```js
// 판정 장면이면 선택지를 감추고 입력창만 — 모델이 실수로 선택지를 줘도 앱이 판단
if (gameState.combat) { enterInputMode(); return; }
```

**(4) 관측 로그**
```js
if (gameState.combat && choices && choices.length)
  console.warn('[combat] 판정 턴인데 모델이 선택지를 보냈다:', choices.length);
```
이 한 줄로 "모델이 얼마나 틀리는지"를 처음으로 셀 수 있게 된다.

#### 영향 범위

| 구분 | 내용 |
|---|---|
| **코드 변경** | 4곳 — 출력 스키마 1줄 추가, choices 규칙 1문단 교체, `processResponse` 수신 1줄, `showChoices` 분기 3줄 |
| **프롬프트 변화** | 순증 약 +60자(필드 설명), 순감 약 −200자(2383·2447행 중복 조항 축약 가능) → **실질 감소** |
| **동작 변화** | 판정 장면에서 선택지를 숨기는 주체가 "모델의 빈 배열"에서 "앱의 combat 플래그"로 이동. 모델이 실수해도 앱이 최종 결정 |
| **기존 세이브** | `gameState.combat`이 없으면 `undefined` → falsy → 기존과 동일하게 동작. **마이그레이션 불필요** |
| **다른 스토리** | `combat` 미사용 스토리(연애·일상)는 모델이 이 필드를 안 보내므로 항상 falsy → 영향 없음. 단 스키마에 필드가 추가되므로 모든 스토리의 프롬프트에 60자가 실림 |
| **회귀 위험** | **중간**. `gameState.combat`이 true로 세팅된 뒤 전투가 끝났는데 모델이 false로 되돌리지 않으면 선택지가 영영 안 나온다 → **반드시 안전장치 필요**: ①"전투가 끝나면 반드시 false로 되돌려라"를 스키마에 명시 ②앱 측 백스톱 — combat이 true인 채로 N턴(예: 8턴) 지나면 자동 해제 + 콘솔 경고 |
| **검증 방법** | 노드 시뮬 — combat=true 턴에서 `showChoices`가 입력창만 여는지, false 복귀 후 선택지가 정상 표시되는지, 백스톱이 8턴에 발동하는지 |

#### 권장 순서
1. 안전장치(백스톱)를 **먼저** 넣고
2. 필드 수신 + 앱 분기
3. 규칙 문구 교체(이때 2383·2447행 중복 축약)
4. 관측 로그로 며칠 플레이 후 오작동 빈도 측정 → 규칙 문구 조정

---

### 개선안 2. 중복 규칙 4종 통합 (세계 능동성·복선·완급 리듬·비밀 단계·이름 창작 금지)

#### 문제점

같은 주제의 지시가 프롬프트 여러 곳에 흩어져 **미묘하게 다른 말로** 반복된다. 원 감사가 지목한 것은 5종이고, 그중 CG 씬키 이중 포함만 처리했다(v326).

이게 만드는 실제 문제:

1. **모순이 조용히 생긴다.** 이번 감사에서 확인된 것만 해도 choices 규칙(2곳 정면충돌), 친밀도 사다리(3곳 다른 경계), 대필금지 vs 독백(2곳 충돌), 능동성 vs 무명원칙(2곳 충돌) — **전부 같은 주제가 여러 곳에 적힌 결과**다. 이름 창작 금지는 원 보고서가 "3곳 중 1곳은 반대 지시"라고 지적했다.
2. **고칠 때 한쪽만 고치게 된다.** 이번에도 내가 친밀도 경계를 상태머신에서 40으로 바꿀 때, 규칙문도 같이 고치지 않았으면 3중 충돌이 2중 충돌로 바뀌기만 했을 것이다.
3. **토큰이 중복 소모된다.** 정적 규칙 총량이 실측 약 77,000자인데, 중복분이 그중 일부를 차지한다.

#### 해결방안

**단계 1 — 실측 (선행 필수, 반나절)**
각 주제별로 실제 위치와 문구를 전수 추출해 표로 만든다. 임의로 통합하지 않고 **먼저 무엇이 어떻게 다른지 확정**한다.

```
주제         | 위치      | 현재 문구 요지        | 다른 곳과의 차이
세계 능동성   | 2400 외 2곳 | ...                  | ...
복선         | 2곳       | ...                  | ...
완급 리듬     | 2곳       | ...                  | ...
비밀 단계     | 4곳       | ...                  | ...
이름 창작 금지 | 3곳       | ...                  | ★1곳은 반대 지시
```

**단계 2 — 주제별 판정**
각 주제를 세 갈래로 분류한다.
- **(a) 완전 중복** → 한 곳만 남기고 삭제
- **(b) 조건부 차이** (예: 스토리별로 다른 게 맞는 경우) → 조건을 명시해 한 문단으로 병합
- **(c) 실제 모순** (예: 이름 창작 금지 3곳 중 1곳이 반대) → **어느 쪽이 맞는지 유저 확인 후** 결정

(c)는 내가 임의로 정하면 안 되는 영역이다. 게임 설계 의도가 걸린 문제라 반드시 확인이 필요하다.

**단계 3 — 통합 실행**
한 주제씩, 한 커밋씩. 각 커밋 후 문법 검사 + 잔존 검증(grep으로 옛 문구 0건 확인).

**단계 4 — 재발 방지**
`ENGINE_RULES_PROMPT` 상단에 주제별 앵커 주석을 넣어, 이후 규칙을 추가할 때 기존 위치를 먼저 찾도록 한다.
```
// [규칙 인덱스] choices=2325 / 친밀도=2397+4693 / 대필금지=2313 / 능동성=2400 / 이름창작=xxxx
```

#### 영향 범위

| 구분 | 내용 |
|---|---|
| **코드 변경** | `ENGINE_RULES_PROMPT`·`ACADEMY_LORE` 등 프롬프트 문자열 다수 (주제당 2~4곳 × 5주제 = 약 14곳) |
| **프롬프트 변화** | 중복 제거로 순감 예상. 정확한 양은 단계 1 실측 후 산출 가능 |
| **동작 변화** | **모델 출력의 성격이 바뀔 수 있다.** 규칙 문구가 줄면 그 규칙의 준수 강도가 달라질 수 있어, 통합 후 몇 턴 플레이해 체감 변화를 확인해야 한다 |
| **회귀 위험** | **높음**. 이번 감사에서 확인됐듯 각 조항은 과거의 구체적 유저 지적에 대응해 추가된 것이다(주석에 날짜와 사유가 남아 있다). 뉘앙스를 잘못 합치면 **그 옛 문제가 되살아난다** — 실제로 choices 규칙이 정확히 그 사례다 |
| **되돌리기** | 프롬프트 문자열 변경이라 git revert로 즉시 복구 가능. 코드 로직 변경이 없어 세이브 호환성 영향 없음 |
| **작업량** | 단계 1(실측) 반나절 + 단계 2~3(주제당 1커밋) 1~2일. 단계 2의 (c) 판정은 유저 확인 대기 |

#### 이 작업을 지금 하지 말아야 할 이유 (정직한 의견)

지금 v327은 **실제 플레이로 한 번도 검증되지 않았다.** 44건이 한꺼번에 들어간 직후에 프롬프트 14곳을 더 건드리면, 문제가 생겼을 때 원인이 44건 쪽인지 통합 쪽인지 분리할 수 없다.

**권장 순서**: v327로 몇십 턴 플레이 → 런타임 보류 2건 측정 + 결함 A·B·C 수정 → 안정 확인 → 그 다음 중복 통합.

---

## 8. 요약

| 구분 | 건수 | 내용 |
|---|---|---|
| 존재 검증 통과 | **44/44** | 보고된 수정이 코드에 전부 실재 |
| 적합 판정 | **39건** | 구조 검토에서 충돌·부작용 미발견 |
| **결함 발견** | **3건** | A: `tries` 재시도 증가 / B: `_slotMoved` 소거 순서 / C: `metChars` 장면 인물 미보호 |
| 판단 보류 | 2건 | 요일·사건 스케일 정합성, chronicle 실제 누적 — **v327 플레이 데이터 필요** |
| 유저 확인 필요 | 1건 | `SNAP_MAX` 60→30 (되돌리기 범위 반감, 내가 임의 결정) |
| 미개선 개선안 | 2건 | `combat` 구조화, 중복 규칙 통합 (7절) |

**다음 행동 제안**: 결함 A·B·C는 전부 코드 로직이고 회귀 위험이 낮아 바로 고칠 수 있다. `SNAP_MAX`는 네 판단이 필요하다. 보류 2건과 개선안 2건은 v327 플레이 이후가 적절하다.
